# モーション図鑑 — Motion Zukan

UIが生き物のように動く、感覚のカタログ。

メニュー・検索ボックス・トグルといった定番コンポーネントに宿る「気持ちいい動き」
（スカッシュ&ストレッチ、オーバーシュート、モーフィング…）を採集して、
触って観察できる標本として収録した図鑑です。

## 起動

```bash
pnpm install
pnpm dev
```

`http://localhost:5173` で図鑑が開きます。

```bash
pnpm build    # 型チェック + 本番ビルド
pnpm preview  # ビルド結果の確認
```

## 収録標本（全152種）

| No. | 標本 | 動きの見どころ |
| --- | --- | --- |
| 01 | メニュー | ● ボタン自身がメニューへモーフィング |
| 02 | 検索ボックス | びよーんと伸び、虫めがねがまばたき |
| 03 | セレクトボックス | 選択肢が時間差でぽこぽこ湧き出す |
| 04 | アクションシート | 行き過ぎてからふわっと着地 |
| 05 | 音声入力 | マイクがくねくねと聞き耳を立てる |
| 06 | ボタン | ゼリーのように潰れて跳ね返る |
| 07 | トグルスイッチ | ノブがキャラメルのように伸びて滑る |
| 08 | チェックボックス | 箱がぷるんと喜び、チェックがひと筆書き |
| 09 | いいねボタン | ぎゅっと溜めて、ぼんっとはじける |
| 10 | タブ | インジケーターがガムのように追いかける |
| 11 | ローディング | 落ちて潰れて跳ねる、重さのある行進 |
| 12 | 通知ベル | 揺れ幅が減衰していく、視覚の擬音語 |
| 13 | HPバー | 本体は即減り、ゴーストが遅れて溶ける |
| 14 | コインカウンター | 桁がスロットのドラムのように回る |
| 15 | ダメージ数字 | 跳ねて散らばり、会心はためて3倍で弾ける |
| 16 | 経験値バー | 満タンで光の波が走り、バッジが跳ねる |
| 17 | コンボカウンター | 連打で育ち、途切れると崩れ落ちる |
| 18 | 宝箱 | ガタガタ震えてじらしてから、ぱかっと開く |
| 19 | スキルクールダウン | 影が時計回りに晴れ、回復でぷるんと合図 |
| 20 | カードめくり | 浮遊→勢い余ってひるがえり、光が走る |
| 21 | スタミナ | しずくがぷちんと蒸発し、ぽたっと満ちる |
| 22 | シールド割れ | ヒビが走り、3発目でパリンと砕け散る |
| 23 | 実績トースト | せり上がってメダルがくるんと一回転 |
| 24 | ボス登場の帯 | 帯が交差し、名前がドンと据わって震える |
| 25 | カウントダウン | 残りが減るほど鼓動が速まり、GO!で解放 |
| 26 | おかね不足 | ボタンが「ううん」と首を横に振る |
| 27 | 長押しチャージ | ためた時間のぶんだけ星が大きく遠くへ飛ぶ |
| 28 | バーチャルパッド | ノブが指に吸い付き、離すとばねで跳ね戻る |
| 29 | 引っぱり発射 | 予告ドットが伸び、離すと逆向きへ飛んでいく |
| 30 | フリック仕分け | 傾きでスタンプがにじみ、しきい値超えで飛ぶ |
| 31 | 装備スロットの磁力 | 近づくと吸い寄せられ、かちっと収まって輪が弾ける |
| 32 | フリックルーレット | 指の勢いで回り、摩擦で減速。針が境界をはじく |
| 33 | 扇に開くカード束 | 底辺支点でしゃらっと扇開き、放物線のyが弧を描く |
| 34 | 手札ディールの散らばり | 手置きの不揃いオフセットでぱらっと場に広がる |
| 35 | 階段にせり上がるカード | 待機中の2pxズレが予告し、階段状にすっと立ち上がる |
| 36 | CSSカバーフロー | 選んだ1枚が正面を向き、左右は±38°で奥へ畳まれる |
| 37 | タイムマシンの奥行きスクラブ | 目盛りを撫でると過去が奥からせり出し、手前へ飛び抜ける |
| 38 | 棒グラフの発芽 | 60ms時間差でにょきにょき生え、最大値だけ一拍遅れて咲く |
| 39 | 折れ線のひと筆書き | 光るペン先が線を描き、通過した点がぽつぽつ灯る |
| 40 | ドーナツの注ぎ込み | 弧が時間差で注がれ、%が追走。最後のひと欠けでじらす |
| 41 | メーター針の勢い余り | 針が7°通り過ぎて減衰振動。危険域では鼓動が止まらない |
| 42 | KPI数字の着地 | 高速回転から減速して着地し、前週比バッジが余韻で跳ねる |
| 43 | 目標達成リングの臨界 | 99%でじらし、100%で光の輪とチェックのひと筆書き |
| 44 | 散布図のポップコーン | 原点側から順に弾け、外れ値だけ最後に大きく跳ねて主張 |
| 45 | ヒートマップの熱伝導 | 熱源から距離ぶん遅れて色が波及し、熱源だけ鼓動し続ける |
| 46 | エリアチャートの満ち引き | 水位が左から右へ抜けるように入れ替わり、たぷんと落ち着く |
| 47 | レーダーチャートの変身 | 頂点ごと30msずらしでぐにゃり。伸びた頂点は光り、縮んだ形は残像 |
| 48 | ファネルの滴り | しずくが次の段へ注がれ、こぼれた分は霧になって散る |
| 49 | スパークラインの心電図 | 静かに流れ続け、異常値が来た瞬間だけピクッと跳ねて光る |
| 50 | 収支の橋 | 前の柱の肩から渡り、増減は伸びる向きで語る。期末だけ立て直す |
| 51 | 持ち分の希薄化 | 新しい層が楔で割り込み、押された層はたわんでから譲る |
| 52 | しきい値越えのドキッ | 跨いだ瞬間だけ線がびりっと震え、越えた区間が濃く明滅する |
| 53 | 予約と一斉確定 | 予約札が破線で呼吸し、確定で順に押印が降りて束が締まる |
| 54 | 総量固定の配分 | 増やすと必ず誰かが減る。総量バーは伸びず光だけが横切る |
| 55 | 前週比矢印の感情 | 上は跳ねて濃く、下は跳ね返さず沈む。横ばいは迷ってから水平 |
| 56 | クエスト目標の差し替え | 達成した札が上へ抜け、次の札が下からせり上がって席を継ぐ |
| 57 | 新機能アンロック | 条件が灯るたび掛け金が上がり、揃うと錠が落ちて扉が開く |
| 58 | 注釈ピンの打刻 | ピンが加速して刺さり、波紋のあと札がひらっと開く |
| 59 | 予告と発効 | 予告の紙は端が揺れ続け、発効の瞬間ぴたりと止まって濃度が入る |
| 60 | 確定と含みの二層 | 含み分だけが呼吸し、確定でスナップして止まり実線に落ちる |
| 61 | 残りの砂 | 落ちる速さが焼け方。黒字で止まり、ひっくり返そうとしても戻る |
| 62 | データ点の磁力ツールチップ | 最寄りの点へ吸い付き、ガイドが先・値が後で追う |
| 63 | 円グラフの切り分け | 出は速く行き過ぎ、戻りは粘って吸い付く |
| 64 | サンキーの水流 | 粒が流れ続け、なぞった経路だけ速く濃くなる |
| 65 | 因果のリレー | 原因の区画が押され、その結果の帯だけが±0から一斉に伸びる |
| 66 | 数値行の物差し | 行ごとの物差しが満ち、動いたぶんが継ぎ足しで灯る／斜線で残る |
| 67 | 取り消し猶予のほどけ | 縁を縫う糸が猶予のぶんだけほどけ、尽きると縁が締まって確定する |
| 68 | 効き始めるまでの影 | 押しても線は動かない。未来側に置かれた影へ、週が近づいていく |
| 69 | 楽観のあと出し訂正 | 先に効いたように動き、失敗が返るとたわんで引き剥がされる |
| 70 | 保留の行列 | 返事待ちが溜まるほど、列が長くではなく深くたわむ |
| 71 | さかのぼり訂正 | 確定済みの過去が書き換わる。旧い値は縁だけ残る |
| 72 | よその手が入る | 他人のカーソルが遅れて滑り、触っている行だけ順番を譲る |
| 73 | スクロールのバトン渡し | 再生ヘッドが手の中にある。行き過ぎずに姿を渡していく |
| 74 | 推定の狭まり | 中心は描かない。測るほど狭まる帯だけが確度を語る |
| 75 | 利子のかかる速さ | 急いだぶんだけ層が溜まり、以降ぜんぶの動きが鈍くなる |
| 76 | 見飽きたぶんだけ短くなる | 受け取るたび前置きが削られ、着地だけが最後まで残る |
| 77 | 同時に鳴ったときの順番 | 3つ同時の変化に、先に動いたかどうかで主役を配る |
| 78 | 複利の雪だるま | 同じ「積む」の増分が回ごとに育ち、止めると自重で沈む |
| 79 | 積み上げ棒の並び替え | 遠い棒ほど高い弧を描き、重い棒ほど深く潰れて着地 |
| 80 | 紙の押し込み | 一瞬で1px沈み、繊維が戻るようにゆっくり浮く。跳ねない |
| 81 | 行きかけて、やめる | 開き切る前に押し返すと、いま居る場所から残りの距離ぶんで引き返す |
| 82 | 同じ返事は束ねる | 連打しても拍は1つ。何回来たかは振幅だけが語る |
| 83 | 滑っているものを掴む | 惰性で滑る帯が、触れた瞬間その場で止まる |
| 84 | 同じものが場所を変える | 一覧の1枚が詳細の見出しへ昇格する。一度も消えない |
| 85 | 骨から身へ | 骨は実体の型。届いた行から順に身へ変わり、行は1pxも動かない |
| 86 | 抜けたあとの席 | 先に抜け、ひと拍おいて席が閉じる。下の行はその結果として上がる |
| 87 | 行き先が画面の外 | 端に着く前に消え終わり、代わりに残った縁がその一点だけ凹む |
| 88 | 動かさずに同じことを言う | 潰していいのは尺と反復だけ。量は個数へ、跳ねは段階へ翻訳する |
| 89 | 見ていないあいだに終わったこと | 戻った瞬間もう最終値。再生の代わりに、時間で消えない跡が残る |
| 90 | まだ見えていないところで増える | 読んでいる行は1pxも動かない。増えたことは縁の気配だけが言う |
| 91 | 跡が溜まりすぎたとき | 束ねずに畳む。落とすのは詳しさだけで、件数は1件も落とさない |
| 92 | フォーカスはどこから来たか | 飛べるのは隣まで。連打中は一度も飛ばず、現在地は動きを待たない |
| 93 | 読んでいたものが消える | 中身だけ消えて席は残る。詰まるのは時間ではなく、触れたとき |
| 94 | 連れて行かれる | 移動は尺ゼロ。経路のかわりに、出発地へ消えない印を置く |
| 95 | 現在地が2つあるとき | 囲む・塗る・指すで分ける。状態の現在地だけは強弱を変えない |
| 96 | 絞り込みの外に出る | 席は残さない。可逆性は行ではなく条件の側に載せる |
| 97 | 戻り先が変わっている | 座標に誤差0で戻ると、別の行に着く。保存するのは行 |
| 98 | 自分のじゃない現在地 | 他人は行の外に、遅れて届く。他人は主役にならない |
| 99 | 自分の現在地を人に送る | 送るのは行。指し直せなかったことは受け手にだけ言う |
| 100 | 入れ物のほうが変わる | 掴んでいる間、読みかけは1pxも動かない。座標だけが嘘になる |
| 101 | 昨日の続きから | 行は壊れていない。確度だけが落ちる。持っていても適用しない |
| 102 | まだどこにも居ない | 「無い」は名乗らせる。最初の1つは移動ではなく、その場に湧く |
| 103 | 現在地に幅があるとき | 塗るは範囲、指すは作用点。反転は消えずに向きを返す |
| 104 | 行のない現在地 | 座標ではなく近傍の名前＋相対。不動点は読み手が指した点 |
| 105 | 自分で動く現在地 | 主語が2人なら担体も2つ。時間を写す塗りに緩急を付けない |
| 106 | 見えていない現在地 | 端の印は現在地ではなく方角。動いていないものを動きで語らない |
| 107 | 窓が2つあるとき | 現在地は台帳のもの、見え方は窓のもの。追うのは頼まれた窓だけ |
| 108 | 畳まれた中の現在地 | 内側に方角は無い。代弁は別の担体で、段数だけを言う |
| 109 | 逃げ続ける末尾 | 末尾は行ではなく状態。追いついたのに未読がある、が言える |
| 110 | まだ手元に無い現在地 | 位置が分からないなら位置を名乗らない。骨に現在地を重ねない |
| 111 | 一度に20個変わる | 順番は因果で決める。間が段を語り、変わらなかったことも拍が言う |
| 112 | 履歴の中の現在地 | 戻るは移動ではなく巻き戻し。台帳に跡を増やさない |
| 113 | もう一度見せて | 動きは1msも変えず、いつの出来事かは枠が言う |
| 114 | まだ起きていないほうの動き | 事実の担体は動かさない。予告は行き先だけで、道中を描かない |
| 115 | 二つの予告を並べる | 同じ形・同じ濃さ・同じ原点。分けるのは名前だけ |
| 116 | 戻せない操作 | 差は履歴の列にしか出ない。戻せないは点が増えないことで言う |
| 117 | 届いた予告はもう古い | 待機で消さず、動かさない。鮮度は薄さではなくズレの長さで言う |
| 118 | 予告どおりに来なかった | 外れをUIが名乗らない。外れは次の予告の幅になる |
| 119 | 何もしなかったことで閉じる | 起きなかったことは動かせない。動くのは現在地の縦線だけ |
| 120 | もう予告できない | 境目は閾値ではなく、予告が台に収まらなくなるという幾何で決まる |
| 121 | 先に取られていた | 駒は枠に一度も入らない。履歴に載るのは「取れなかった」 |
| 122 | 届いたかどうか分からない | 線が結果の欄に届かないまま止まる。スピナーを1つも持たない |
| 123 | あとから答えが来る | 線は1pxも動かない。動くのは、知ったことの担体だけ |
| 124 | どれが効いたのか分からない | 候補は破線の枝の個数で言う。効果を運ぶ実線は常に1本 |
| 125 | 読み手が答えを埋める | 減るのは値ではなく幅。申告は破線、測定は実線 |
| 126 | 原因が画面に無い | 候補の台の締め線が外れる。効果を運ぶ実線は消えないし動かない |
| 127 | ほとんどが申告になる | 数字は1文字も動かない。変わるのは台の下辺の成分だけ |
| 128 | 定義が途中で変わった | 目盛りが継ぎ目を横断しない。線は既定と対照で完全に同じ |
| 129 | 過去のほうが変わった | 書き換えを1フレームも再生しない。跡は前に見た読み手にだけ出る |
| 130 | 申告の上に積んだ申告 | 線種は二値のまま。深さは台の下辺の本数が運ぶ |
| 131 | そろっていない現在 | ズレは台の右端の不揃い。合計はいちばん古い材料に揃う |
| 132 | 窓の違う2つを比べる | そろえると比べられる。そろえた分だけ、新しい材料を捨てる |
| 133 | 捨てた分岐 | 起きたのに、いまの世界には無い。消さず・薄めず・動かさず |
| 134 | 入れきれないので間引く | 落としていいのは点の個数だけ。拡大しても頂点は動かない |
| 135 | 捨てたものが場所を取る | 溜まったことは占有面積が言う。片付けるのは読み手だけ |
| 136 | レートは後からしか分からない | 空の場所は実線の箱。確定しても完了の印は置かない |
| 137 | 値段を上げたのは自分 | 自分の分は台帳の点と同じ形で並ぶ。外の分とは別の担体 |
| 138 | 値段の一部だけが分かっている | 既知は離散のチップ、未知は実線の箱。合計は数ではなく長さ |
| 139 | 先に使ってしまった未来 | 埋まっていることは、空きの担体が無いことだけが言う |
| 140 | 決めたのは、3週前の自分 | 予定は1pxも動かない。動くのは現在地の縦線だけ |
| 141 | 予定のほうがずれる | 中割りが出た側が、動いた側である |
| 142 | 同じ日に、別々の理由で来る | 形は分けない。分けるのは行 |
| 143 | いつ来るかは、幅でしか決まっていない | 幅と長さは、空きの減り方だけが分ける |
| 144 | その空きは、誰のものか | 列が決まらない占有は、空きを消さない |
| 145 | 同じ週の、どちらが先か | 順序は、履歴に載る順が言う |
| 146 | 予定より先に、続けられなくなる | 可否は、空きが言う。動くものを増やさない |
| 147 | その約束を、まだ知らない | 画面は説明しない。無反応が定義を教える |
| 148 | 1週ぶんでは足りない予定 | 量は幅ではなく個数。続くことと重いことは消し方が分ける |
| 149 | 半分だけ済んだ | 1回の操作は1つの点。済んだかは、残りが言う |
| 150 | 何も起きないことが、2つある | 届いていないことは、不在ではない |
| 151 | 1粒に満たない | 満たない量は、粒にせず場所として置く |
| 152 | まだなのか、もう過ぎたのか | 不在に理由は書けない。書けるのは場所の側 |

No.147〜149 は、**はい／いいえで返らない問い**を撃った回です。
前回（No.144〜146）は「位置の語彙が原理的に届かないところ」を3方向から撃ち、足りないぶんを
**すでに在る別の語彙に渡す**ことで解きました（主語は台帳が、順序は時間が、可否は空きが言う）。
ただし**渡し先がどれも二値だった**ことは、まだ誰も疑っていませんでした。空きは 0/1、履歴に載るか載らないか、
置けるか置けないか——**この語彙圏は146種を、二値で持ちこたえてきた**わけです。

| No. | 問い | 二値が壊れる理由 |
|---|---|---|
| 147 | **読めるか** | 読み手が語彙を知っているかは、画面が**観測できない** |
| 148 | **置けるか** | 空きは二値だが、予定には**量**がある |
| 149 | **済んだか** | 払えた額が一部なら、成功でも失敗でもない |

**この回の答えは、3種とも同じ形になりました——問いに答えない。答えるのは、在るもの・残っているものの側である。**
147 は無反応が定義を教え、148 は必要な個数と在る個数が最初から同じ画面に並んでいて、149 は残った粒が言う。
No.146 の「可否は、空きが言う」を、そのまま3方向へ伸ばした形です。

- **No.147 その約束を、まだ知らない** → 146 が最後に正直に書いた限界（読めるのは語彙に触れている読み手だけ）の
  続きであり、**146種ぶん積んだ語彙そのものへの問い**です。素直な答え「教える」は4方向から封じられています
  ——凡例・注記・吹き出し・初回だけの案内は**全部「文章で言う」**にあたり、動きで教えれば**教えたことが出来事になり**、
  2回目以降は**予告として読まれ**、そもそも**画面は読み手が知っているかを観測できない**。
  既定は**教えません**。空きの点が無い週7で `置く` を押した前後、ページ内**全200要素**の差分は **0.000px**・
  スタイル差 **0件**・履歴 **±0**——**無反応が、それ自体で定義を提示します**。しかも同じ操作の
  1回目と5回目のスナップショットは**互いに完全一致**（内部の `data-attempts` は **1 → 5** なのに画面は 1px も違わない）。
  **読み手が学習しても、画面は学習しない。** そして規則は、画面が一度も口を開かないまま実演されます——
  `足す` で空きが **5 → 6個**（既存5個の `left` 差分は**すべて 0.000px**）になると、
  **さっき何も起きなかった週6に、今度は置ける**
- **No.148 1週ぶんでは足りない予定** → 量を幅で描きたくなりますが、**横幅はこの語彙圏で三重に埋まっています**
  （No.138 の占有長、No.143 の幅、No.145 の幅）。**四重目は取りません。** 答えは No.88「量は個数へ翻訳する」を
  借りて、**予定チップが必要な個数ぶんの粒を持つ**こと。粒と空きの点は **6×6px**・`border-radius` **50%**・
  色・**間隔 24px** まで一致します（数値を揃えたのではなく、**同じ1つのクラスを `data-role` で出し分けている**）。
  **続くことと重いことは、消し方が分けます**——粒3個の予定は週 **2 / 4 / 6** に**飛び飛びで**載り
  （No.143 の稼働は隣り合う空きしか消せない）、それでも残った空きの `left` 差分は **0.000px**。
  足りないときは**一部だけ置かず、何も起きません**（全204要素の差分 **0.000px**・履歴 **±0**）
- **No.149 半分だけ済んだ** → 結果を二値で受けてきた語彙（No.116 は点が増えるか、No.122 は塗りか輪郭か）に、
  **どちらでもない結果**が来ます。点を半分塗るのは**塗りの量を担体にする**ことなので取れません。
  既定は**点を分けません**——**3個ぶん払えた点と1個ぶん払えた点は、`6×6px`・`rgb(61,61,61)`・`solid` まで一致**し、
  computed style の distinct は **1通り**（内部の `data-paid` は **3 と 1** なのに画面には差が出ない）。
  **済んだかは、残りが言います**: 払った前後で予定チップの `left`/`top`/`width`/`height` は**すべて 0.000px**、
  変わるのは**粒の個数だけ（4 → 1）**。全部済むと粒が 0 個になり**チップが消える**——
  **「済んだ」を言う担体は、チップが無いことだけ**です

この回の設計上の収穫は、**同じ「足りない」でも、担体が分割できるかどうかで答えが逆になる**と分かったことです。
148 の**場所**は分割できないので「置くか、置かないか」しかなく、足りなければ**何も起きません**。
149 の**支払い**は分割できるので、足りなければ**足りるぶんだけ済みます**。
二値の担体（空き）と分割できる担体（原資）が、同じ「足りない」に別々の答えを要求します。

実装の側では、**同じ失敗が3度目**でした。**No.148 と No.149 は、数値条件 C1〜C8 を全項目通したまま、
キャプションで主張そのものを名乗っていました**（「空きの点が足りなければ、何も起きない」／
「残る粒が、済んでいないことを言う」）。No.118 でも同じ直しをしています。配線側が実物を見て両方直しました。
**動きで言うと決めた主張は、文章を1行足すだけで無効になる**——そして**実装者は、主張を1行書き足したくなります**。
3度目なので、もう偶然ではありません。

測る条件の側にも1つ増えました。**ボタンの `hover: translateY(-1px)` が `getBoundingClientRect` に
1.000px のニセの差分を作ります。** 手本にした No.146 や No.143 のボタンも同じ transform を持っていますが、
あちらには「操作前後で座標不変」を測る条件が無かったので問題化していませんでした。
**測る条件を足すと、それまで無害だった演出が壊れる側に回ります。**
CSS の罠がもう1つ: **先例のトースト（`position: absolute; bottom: 4px`）を座標系ごと引き継ぐと、
行数が可変な標本ではバッジ行と完全に重なります**（先例は固定高のキャンバスでした）。

No.144〜146 は、**位置は、そこまでしか言わない**を撃った回です。
前回（No.141〜143）は「確定した未来は位置で言うしかない」の耐久試験で、位置が**動く**・**共有される**・
**一点に定まらない**の3方向とも位置で言い切りました。ただし前回の答えは、自分の限界も一緒に書いています
——**占有の語彙は「何個ぶんか」は言えるが、「どの週か」も「誰のか」も言えない**。
この回は、**位置の語彙が原理的に届かないところ**を3方向から撃ちました。

| No. | 位置が言えないこと |
|---|---|
| 144 | **誰が** — 空きが1つ消えている。消したのが誰なのかは、どこにも書いていない |
| 145 | **どちらが先か** — 同じ週に2件載ると、位置は同率としか言えない |
| 146 | **成立するか** — 位置は正しいのに、そこへ届く前に原資が尽きる |

**この回の答えは、位置を拡張しないことでした。** 足りないぶんは、すでに在る別の語彙に渡します
——**主語は台帳が、順序は時間が、可否は空きが言う。**

- **No.144 その空きは、誰のものか** → 前回が置いた「幅のある予定は空きを1つだけ消す（右端に置く）」を
  **この回が撤回**しました。確定前は週6・7・8の空きが **3個とも在り**、`いま来た` で消えるのは
  **1個だけ・確定した週**です（帯は `left`/`width` とも 0.00px）。**読めないものは、置かない。**
  空きの点は7個で computed style distinct **1通り**（対照は **3通り**に割れます）、
  ずらしの最中も総数は**全サンプルで 7 のまま**、戻った空きは未占有の空きと**属性まで完全一致**します。
  主語は見た目には出ず、**履歴に載るか（読み手 +1 / 確定 +0）だけが言います**。
  実装が塞いだ穴が1つ、**CSS のコメント内の `*/` が基底ルールを CSSOM ごと消していた**のが1つ
  （「数値条件は全部通るのに絵が壊れる」の6回目で、原因は初めて見る形でした）
- **No.145 同じ週の、どちらが先か** → 縦は No.142 が由来に、週の分割は定規そのものに、
  番号・濃淡は No.118 に封じられていて、**残っていたのは時間だけ**でした。
  順序が決まっている週6は履歴の2点が **140.20ms** 差で増え、決まっていない週8は **0.00ms**（同一コミット）。
  **順序が無いことを、書かないことではなく、同時に載ることで言います。** 位置は同率のまま
  （2件の中心 x の差 **0.0000px**）、履歴の2点は distinct **1通り**で先着が大きくも濃くもなりません。
  順序に依存する値（`底`）は決まっていない週だけ**幅**になります——新しい語彙は足さず、
  **No.143 の幅をそのまま借りました**（週3/4/6 は 10px、週8 は 30px＝1週ぶん。違うのは `width` だけ）
- **No.146 予定より先に、続けられなくなる** → 「成立しない」は予定の担体には書けません
  （薄い・破線・消す・赤いは全部予約済み）。答えは**担体を足さないこと**——
  **尽きる週から先は、空き枠が無い**。空きが無い理由が予定なのか原資なのかは見分けが付きませんが、
  **No.144 が「空きは理由を持たない」と決めたのだから、それは語彙どおりです。**
  3つめの動くものは**動かさずに描きました**：空きの点の `left` は生成時から不変（差 0.00px）で、
  変わるのは個数だけ（**5 → 4 → 6個**、端からのみ増減）。予定チップは成立時・不成立時で
  `left`/`opacity`/色が**完全一致**します。**受け入れ条件どうしの整合が、企画の誤りを外から捕まえました**
  ——台本の「予定週の空きも消す」を実装すると、台本自身の数値例と C2・C4 の**3つすべてと矛盾**し、
  しかも成立と不成立が原理的に見分けられなくなるところでした

No.141〜143 は、**その予定は、定規の上の一点ではない**を撃った回です。
前回（No.138〜140）が出した結論は「**確定した未来は、線種ではなく位置で言うしかない**」でした。
線種はもう埋まっている（破線＝**不確か**／実線の箱＝**もう起きたが未知**／実線のチップ＝**確定した未来**）ので、
残っていたのが位置だった、という答えです。この回はその答えの**耐久試験**にしました。

| No. | 「位置で言う」の、どこが壊れるか |
|---|---|
| 141 | 位置が **動く** — 時間が進んだのか、予定がずれたのか、絵が同じになる |
| 142 | 位置が **共有される** — 同じ週に3つ載ると、位置は由来を言えない |
| 143 | 位置が **一点に定まらない** — 来るのは確定なのに、置くべき点が無い |

この回の縛りも企画の段階で決めました。**線種を新しく足さない**（既定の `border-style` は
`solid` / `none` だけ。「ずれた」も「幅がある」も、来ること自体は確定しているので破線を借りない）、
**主張を文章・警告色・バッジで言わない**、**座標は1つの関数から出して Grid の列を全行で共有する**、
そして**移動は `transition: left 0.12s linear` の等速**（`animation` は使わない。跳ねさせた瞬間に、
ずれが「祝うべき出来事」になる）。3種のうち2種で、この `transition` の実測値は文字列一致します。

- **No.141 予定のほうがずれる** → 芯は**動いた側にだけ中割りを与える**ことでした。時間が進むとき
  現在地の線は**尺ゼロで飛び**（16ms サンプルで distinct **2値**・中間値 **0個**）、予定がずれるときは
  そのチップだけが**等速で運ばれます**（distinct **9値**）。**中割りが出た側が、動いた側である。**
  跡は予定の定規に残さず（ずれ後も予定行は **3 チップのまま**、ゴースト **0**）、履歴の行に残します
  ——**元の予定日は一度も来ていない**からです。主語は動き方では言わず、**履歴に点が増えるかどうか**
  だけが言います（読み手のずれ **+1** / 外のずれ **+0**、両者の `transition` は **0.12s / linear** で一致）。
  実装が企画の穴を実測で2つ見つけました。隣接する予定へずらすと占有が潰れて**空きの総数が 6 → 7 に増え**
  （空きの行は週ごとに0/1の二値でしか占有を言えないので、構造的に破れます）、
  既定のキャプションに書いた「1週ぶん**ずれる**」が、**企画自身の禁止語リストで禁じられていました**
- **No.142 同じ日に、別々の理由で来る** → **形は分けず、行を分ける**のが答えです。10個のチップは
  寸法・色・角丸・`opacity` まで distinct **1値**で、3行が**1つのトラック列**を共有します
  （チップ中心と `chipX(week)` の差は10個すべてで **0.0000px**）。**由来は縦が言い、時期は横が言う。**
  合計も件数も出さず（禁止語 **0ヒット**、引き出し線 **0本**、バッジ **0個**）、今週の重さは
  **縦に並んだチップの数**が言います（y 間隔 **27.000px** で最小＝最大、現在地の線との重なり **0.000px²**）。
  行に開いてあるので束ねる必要がなく、1件を押すと動くのは **1個だけ**（**30.00px**、他は全部 0.00px）。
  対照では**企画のほうが壊れました**——「由来ごとに形を変える」と「同じ週を束ねる」を同居させると、
  家賃の週が常に他の由来に取られて**家賃の三角形が一度も画面に出ません**でした
- **No.143 いつ来るかは、幅でしか決まっていない** → 幅は**同じチップを伸ばした1本の帯**で言います
  （帯とチップの線種・色・高さ・角丸は文字列一致、違うのは `width` だけ。既定の `dashed` **0件**）。
  芯は**幅（いつか分からない）と長さ（続く）が同じ担体になってしまう**ことで、分けたのは**占有**です
  ——**続くものは3週ぶんの空きを消し、幅のあるものは1つしか消さない**（同じ 90px の帯で、空き週は
  既定 **4個** / 対照 **2個**）。幅は縮まず、線が帯を貫くだけ。確定しても**帯は消えず、チップが1つ増える**
  だけです（帯の `left`/`width`/`opacity` は確定前後で文字列一致、担体総数 **3 → 4**）。
  実装が実物で2つ見つけました。**確定チップが帯に埋もれて見えず**（数値条件を1つも破らずに起きました）、
  そして**「仮の押さえ」がどの週に居るのかは読み手には読めません**——
  **占有の語彙は「何個ぶんか」は言えても「どの週か」を言えない**、というこの回の限界です

No.138〜140 は、**まだ来ていないのに、もう決まっている**を撃った回です。
図鑑はここまで「これから」を **1種類**としてしか持っていませんでした——No.114 の破線（**不確か**）と、
No.136 の実線の箱（**もう起きたが、まだ知らない**）の2つです。ところが**確定した未来**が実在します。
規則で決まる金額、借りた返済、もう押さえられている枠。**予告の破線を借りると読み手は疑って備えず、
事実の実線を借りると「もう起きた」になります。**

| No. | 何が「もう決まっている」のか |
|---|---|
| 138 | **量**の側 — 1つの数字の内側で、既知（規則）と未知（観測待ち）が混ざる |
| 139 | **場所**の側 — 借りた瞬間に、これから5週ぶんの枠が埋まる |
| 140 | **時間**の側 — 決まっていた未来が、現在になる |

この回の縛りも、企画の段階で決めました。**確定した未来を破線で描かず、確定していることを
文章・警告色・強調・注記で言わない。** 言えるのは**位置・占有・同じ形の反復**だけです。
確定した未来の担体に `animation-name` を持たせないことも足しました（No.114 の尺ゼロの継承。
**動かした時点で「起きた」になる**）。

- **No.138 値段の一部だけが分かっている** → まず決めたのは**新しい担体を発明しない**ことでした。
  「観測待ち」には図鑑がすでに答えを持っている（No.136 の実線の箱＋幅0の塗り）ので、外の分はそれを
  そのまま借り、**新しく決めるのは「規則で決まる部分」の担体だけ**にしています。自分の分は
  **過去からの予測ではなく、台帳に在る行の数え上げ**なので No.137 のチップを継ぎました
  （x の差 **0.00px**。外の分の台本を**10倍**にしても結果は**バイト単位で一致**＝レートを一度も参照していない）。
  **合計は数として合成せず、チップと箱を同じ1本の並びに置いて占有長で語ります。**
  企画の式（箱の幅＝過去の最大値）は**台本の2件目で破れ**、はみ出しをクリップすると
  **予約不足の箱が「ぴったり埋まった箱」に見え**ました。正直に出しても、塗りが枠線を塗りつぶして
  **箱がどこで終わったのか読めない**まま——最終形は塗りを下・枠線を上にして 1px の白いハローを敷き、
  **器の右端と塗りの先端が別々に読める**ようにしています
- **No.139 先に使ってしまった未来** → **新しい線種を足さない**と決めたのが芯です。
  **埋まっていることは、空き枠の担体が存在しないことだけが言う**（No.121「在るはずのものが無い」の、
  未来の側での再演）。既定の未来側の `border-style` は distinct **`[none]`** の1値のみで、
  借りると空き枠 **10 → 5 個** / チップ **0 → 5 個**。No.119 の「読み手は履歴に、時間は定規に」が
  割れそうになるところは、**担体の形が主語を言い、担体の位置が時制を言う**で両立させました。
  5つのチップは `animation-name` **5/5 とも `none`** で同時に現れます——**1回の操作は1つの出来事**。
  完済すると定規は空きに戻り、**履歴のチップは 10 → 10 で1個も減りません**
- **No.140 決めたのは、3週前の自分** → 図鑑は「未来をどう描くか」を何度もやりましたが、
  **未来が現在になる瞬間**を一度も扱っていませんでした。**予定どおりに来たものは、
  何も起きていないように見えます**——動かすと「起きた」が強すぎ、動かさないと通過に気づかない。
  答えは**動くのを1つに絞る**ことで、1クリックで動いた要素は**ちょうど 1 個**（現在地の縦線）、
  他のすべては **0.00px**。しかも通過数が増えても動く要素は増えません（0/1/3連打で **0/1/1 個**）。
  由来は**履歴の列と予定の列が同じ x 定規を共有していること**だけで読ませます
  （チップ中心が全週・両行で `chipX(week)+130.00` ちょうど＝**系統誤差 0.00px**）。
  週7には何も来ていませんが、画面はそれを一言も言いません

No.135〜137 は、**その交換は、1回では終わらない**を撃った回です。
前回（No.132〜134）は交換関係そのものを扱いましたが、**3種とも交換を1回きりの、閉じた、
独立な出来事として扱っていました**。押したらそこで終わる、という前提です。この回はそこを壊します。

| No. | 交換の、どこが終わっていないのか |
|---|---|
| 135 | **場所**の側 — 捨てたほうが残り続けて画面を食う |
| 136 | **時間**の側 — 値札が、払う時点に存在しない |
| 137 | 交換どうしが**独立でない** — 1回の交換が次の交換の値段を変える |

この回を貫いた縛りは、企画の段階で1つ決めました。**交換に「完了」の印を置かない**——
チェックマーク、`完了しました`、成功トースト、確定表示、履歴に1点積んで閉じる、をすべて使いません。
これは前回の縛り（**交換レートを画面が決めない**）の延長です。完了の印は
「この交換はこれで終わり＝この値段で確定」という**画面による宣言**であり、
**実際には終わっていないものを終わったことにすると、レートを画面が決めたことになります**。

- **No.135 捨てたものが場所を取る** → まず「**捨てた**という関係を運んでいるのはどれか」を決める必要が
  ありました。試すと**値そのものは運んでいません**（「3回目の交換で、何かと引き換えにこれが捨てられた」は
  値を知らなくても読める）。だから畳むときに落とすのは値を**表示する子要素だけ**で、
  `seq`（いつ）と `kept-value`（何と引き換えに）は要素が在る限り手放さない
  （畳みの前後でマス **5 → 5**、値ラベル **5 → 0**、属性は5件とも一致）。**No.91 の「束ねずに畳む」を
  そのまま持ってくると、この標本では関係のほうが落ちます。** 捨て置き場は交換1回で
  **10,404px²**、5回で **22,644px²**、10回で **34,884px²** と実際に育ち、10回で本当に溢れる
  （`scrollHeight` **126px** > `clientHeight` **98px**）。**溢れの扱いを「器がスクロールする」に
  決めたことが、そのまま計測の罠になりました**——`overflow-y: auto` の容器自身の
  `getBoundingClientRect()` は溢れても変化しないので、面積は**中身の要素**を測らないと必ず落ちる。
  既定は自分からは何も減らさず（10回のあいだ単調増加、**5秒放置しても 10 のまま**）、
  減らせるのは `片付ける` を押した読み手だけ。対照は捨てた点を最初から **4×4px / opacity 0.4** と
  弱く描き、2.5秒で **1 → 0 個**に消し、6件で `捨てた: 6` の**バッジ 1 個**に束ねます
- **No.136 レートは後からしか分からない** → **No.74 の帯を使うと、レートが確率変数になります。**
  実際にはレートは確率的なのではなく**まだ観測されていないだけ**で、
  **「分からない」と「ばらつく」は別の事実**です。既定は帯を使わず、受け取り枠を
  **幅 62px 固定の実線の箱**にしました——**No.114 の破線を借りてはいけない**（あれは
  「まだ起きていない」の語彙なので「未来の推定」に読める）。**箱＝交換は起きた（事実）、
  塗り＝レートが分かった（知識）。** 払った直後は塗り **0px**・数字 **0 個**・帯 **0 個**、
  払ったほうは動く（**18.036 → 46px**）のに受け取り枠は20フレームで最大差分 **0px**
  ——**払ったことは事実なので動き、受け取ったことはまだ事実でないので動かない。**
  確定の瞬間に動くのは受け取る担体だけ（他はすべて **(0,0,0,0)**、当該行の塗りだけ **0 → 44.1875px**）。
  3つ並んだ未確定の対応は `RETURN_TABLE[seq]` だけで決めるので **26h → 9h → 33h** が一度も
  入れ替わりません。そして**確定は完了ではない**——チェック・トースト・`確定しました` は **0 個**、
  確定した行は消さずに残る（3回払って3回確定した後も **3 行のまま**）。対照は確認ダイアログ
  **1 個**（No.116 が否定した手）、推定値 **1 個** ＋ 斜線の帯 **1 個**、そして
  `確定しました ✓` のトーストの **1.3 秒**後に行そのものが消えて履歴が **0** に落ちます
  ——**対照では、レートを学ぶための材料が毎回きれいに片付けられていきます**
- **No.137 値段を上げたのは自分** → 単価は **300 → 340 → 400 → 440 → 320 → 290 → 330円**と動きますが、
  上げているのは市場でも時間でも他人でもなく**読み手自身の直前の操作**で、**画面は一度もそれを
  言いません**（「買いすぎ」に相当する語 **0 件**、警告色 **0 件**、`animation-name` は **35 要素すべて
  `none`**）。原因と結果は**1手ずれ**ていて、押した直後20フレームは単価が1文字も変わらない——
  **これはタイマー調整ではなく構造で**、第 r 回の自分の分は `history.slice(0, r - 1)`、
  つまり**いま記録した行動を最初から含まない添字**から計算します。芯は
  **止められる分と止められない分を別の担体に置く**こと。これは No.132「分解できない差は幅のまま出す」の
  **逆の判断**で、逆にできる理由は**因果の出どころが台帳に記録されている**ことです——
  **分解できるかどうかは、台帳に書いてあるかどうかで決まる。** 見送った回に
  自分の分は **120 → 0円**、外の分は **+20 → +20円**で動かない。そして気づきは、
  **自分の分のチップが台帳の買った点と同じ形をしている**ことで起こります
  （`10×10px` / `rgb(61,61,61)` / `border-radius: 2px` が一致し、**x 座標も 184 / 198 / 212px で差 0px**）
  ——**同じ場所に同じ形で積まれ、見送ると同じように消える。UI は1文字も説明していません**

この回の設計上の収穫は、**「終わったことにする」のは、いつも画面の都合だ**と分かったことです。
3種とも、終わったことにするほうが画面はきれいになります（捨て置き場は片付き、確定にはチェックが付き、
値段は1つの数に収まる）。**きれいになるぶんだけ、読み手が次に使える材料が消えていました。**

実装の側では、**数値条件では検出できない不具合**が、この回も3種すべてで出ました。
No.136 は確定した数字を塗りの内側に置いていて、**塗りが伸びるほどラベルの背景を侵食してコントラストが
死ぬ**（値が大きい交換ほど読めなくなる、という主張と正反対の壊れ方）。No.137 は
**内訳行にだけ親の `padding: 8px 9px` が付いていたため、絶対配置の子の座標系が台帳行とズレ、
チップと点の x が 13px 食い違っていました**——**数値で一致させるつもりの主張が、親のパディングで
黙って壊れていた**（前回の「親の `flex` の `gap` が幾何に混ざる」と同じ系譜が、別の入口から出ています）。
No.135 は対照モードで本線の点が1つも出ておらず、C1〜C8 とは無関係だったので数値では気づけませんでした。

企画が答えを持てなかった箇所も1つ、正直に残しておきます。No.135 の
**「溜まったことを、いつ・何が言うのか」**は、実装しても文言でもバッジでも答えられませんでした。
答えているのは**専有面積が実際に育つことと、溢れて縦スクロールが要ること**だけで、
触れば読めますが、**静止画1枚では「箱が小さいだけ」に見える可能性が消えていません**。
No.137 も同じ形の限界を踏んでいて、**「見送ると自分の分だけが戻る」は1枚の絵には写らず、
2枚を並べるか GIF でしか伝わりません**。**蓄積と、行動の帰結は、静止画では主張できない。**

No.132〜134 は、**どちらも正しいのに、両方は取れない**を撃った回です。
図鑑はここまで、どの標本でも「正しい答えが1つある」ことを前提にしてきました。
この回に選んだのは、**2つの良いことが交換関係にあって、片方を取ると必ずもう片方を失う**場面ばかりです。

| No. | 交換されるもの | 交換を起こしたのは誰 |
|---|---|---|
| 132 | 比較可能性 ↔ 鮮度 | **台帳**（材料の届き方。誰も選んでいない） |
| 133 | やり直せること ↔ いまの世界が1本であること | **読み手** |
| 134 | 描けること（密度） ↔ 見落とさないこと（忠実さ） | **機械**（画面の幅という物理） |

この回を貫いた縛りは3つです。**片方を弱めて描かない**（薄く・小さく・遅くすると、読み手はそれを
「劣った状態」と読む＝No.95「担体は1つの事実しか言えない」の交換版）。**交換レートを画面が決めない**
（閾値・推奨・警告を置かない）。そして**捨てたほうを、消さずに残す**。

図鑑には交換を扱った標本が2つありました——No.54「総量固定の配分」は**同種のあいだ**の保存、
No.75「利子のかかる速さ」は**時間差の付け**（いま速く動くと後で鈍る）です。
**別種の良いことが、同時に成り立たない**場面は、この3種が初めてになります。

- **No.132 窓の違う2つを比べる** → **数え方が同じなので目盛りは貫通してよい**（No.128 との撃ち分け。
  既定の目盛りは2行で共通の **1 組**）。違うのは締め日だけで、**差のうちどれだけが実際の変化かが
  分解できない**——出せないのではなく、分解できない。だから差は1つの数にせず**幅**で出す
  （帯 **207.08px**、中心線は **0 個**）。`窓をそろえる` の1クリックで帯が **207.08 → 0px** に潰れるのと
  **同時に**、今週の台の右端が **476.14 → 347.56px** へ引く（遷移中の10サンプルで**片方だけが動く
  フレームは 0 枚**）。**そろえるとは、新しいほうの材料を捨てること。** 捨てた塗りは
  **347.56〜476.13px** に `rgb(61,61,61)` / `opacity 1` のまま残り、台に載っている塗りと**完全一致**。
  対照は差を生の1値 **+¥171,900**（既定の帯の**上限と同じ数**）で言い切り、押しても台は
  **257.14px のまま1pxも動かず**、`最終更新: 今日 09:12` と**新しいほうだけを名乗る**
- **No.133 捨てた分岐** → やり直しは**台帳に点を1つも増やさない**（本線 `7 → 7`。対照は `5 → 6` で
  **+1**＝**戻ったのに前へ進んだように見える**）。増えるのは枝のほうで、**台帳は1本のまま、その外側**に
  残る。枝の点は本線と `rgb(61,61,61)` / `opacity 1` / **9×9px** まで一致し、x も週ごとに一致
  （**243.50 / 301.50 / 359.50px**）——違うのは y（**19.50 → 51.50px**）だけ。そして**枝は一度も
  動かない**（20 サンプルで位置は **distinct 1 通り**）——No.98「他人は行の外に**遅れて滑って**届く」
  との撃ち分けで、**自分の捨てた過去は、行の外で動かない**。現在地は枝に **0 回**乗り、
  捨てた値（145 / 190 / 195）は再演の枠の中でだけ読めて `いまの残高` は **distinct 1 値（215）**。
  対照は枝が **0 個**、トーストが **2.2 秒で 0 個**＝**読み手の記憶にだけ残る数字**ができる
- **No.134 入れきれないので間引く** → 1画素あたり 12.5 点なので「全部描く」は選択肢に無い。
  既定は各画素を **min〜max の縦の1本**で描くので、**1 点しか無いスパイクでも必ず出る**
  （真の最大 **150 / y=5.56px** と描画上の最大が**完全一致**。対照は **63.04 / y=114.99px** ＝
  **109.43px 低い位置にしか線が無い**）。**拡大しても山の頂点は動かない**（既定 **74.77 → 74.77px**、
  差 **0.000px**）のに対し、対照は **114.99 → 74.77px** と **40.22px** 跳ぶ＝**データは1点も
  変わっていないのに、無かった山が生えてくる**。捨てたことを画面は名乗らず（バッジ **0 個**、
  件数は両モードとも **`5,000 件`** の同一文字列）、**帯の太さだけ**が「ここには中身がある」と言う

この回の設計上の収穫は、**交換は、失うほうに担体を置かないと見えない**と分かったことです。
3種とも「得るほう」（比較可能になる・世界が1本に保たれる・絵が描ける）は放っておいても画面に出ます。
**画面に出ないのは、いつも失ったほうです。**

実装の側では、**数値条件を全部通したまま絵が何も言っていない**が、この回もまた出ました。
No.132 の初版は C1〜C6 を全通過していたのに、**揃える前と後で絵がほとんど同じ**でした——
捨てられた塗りが、台の引いたあとの空間をちょうど同じ濃さで埋めるからです。
No.116・No.118 が踏んだ「**担体は、それが載る場所が描かれていないと読めない**」の裏返しで、
ここでは**「台を失った」を言うのに、失われる台そのものが描かれていなかった**。
直しはレールを塗りの**背景ではなく下**に独立した担体として描くこと——
**不透明な塗りの下に敷いた台は、永久に見えない。**

企画の言葉が足りなかった箇所も1つ出ています。No.134 の「落としていいのは点の個数だけ」は、
そのまま実装すると拡大時に `min === max` の列が増え、**隣と値域が重ならない箇所が 248 件**出て
帯が繋がらず、**折れ線が粒の散らばりになりました**。直前の列の値を値域に含める直しで **0 件**になり、
極値は広がる方向にしか変わらないので C1・C2 の実測値は**1つも変わっていません**。
正しい言い方は **「落としていいのは点の個数だけで、値の範囲も、隣とのつながりも落としてはいけない」**
でした。**「落としていいもの」を数え落とすと、担体が切れる。**

No.129〜131 は、**その数字が、いつ・何から出来たのか**を撃った回です。
No.126〜128 が「画面についての事実は、担体ではなく**台**に載る」と決めたのを受けて、
この回は**その台が1枚では足りない**場面ばかりを選びました。

| No. | 台が1枚では足りない理由 | 既定の答え |
|---|---|---|
| 129 | 遡及の跡を載せる台が**世界の側に無い** | 台をもう1枚、**読み手の側**に持つ |
| 130 | 深さを載せる軸が**台の下辺（1次元）に無い** | 台を**積む**（本数＝段数） |
| 131 | 時点を載せる定規が **x に既に居る** | 台を**行ごとに**持つ |

この回を貫いた縛りは、**台は増やしてよいが、線種・色・薄さは1つも増やさない**、です。
線種（実線／破線）は測定／申告（No.125）と届いた／届かない（No.122）で埋まり、
薄さは確度（No.74）で埋まり、色は対照の語彙です。**空いていたのは台の枚数だけでした。**

- **No.129 過去のほうが変わった** → 新しい定義で**過去まで計算し直す**と、No.128 の継ぎ目は
  消えます（横断する目盛りが **0 → 4 本**、締め線が **1 → 0 個**）。**遡及は比較可能性を
  回復する操作で、失われるのは読み手の記憶のほうです。** そして書き換えは
  **1フレームも再生しません**——`ひらく` の前後 20 フレーム、変化する 6 頂点 × 20 の
  **120 サンプル中、中間値は 0 件**（`transition-duration` **`0s`** / `animation-name` **`none`**）。
  対照は同じ窓の **120 サンプル中 96 件**が中間値で、**ずっと前に決まっていたことが
  「いま起きた」**になります。芯は跡の置き場所で、**世界には跡を置く相手が居ません**
  （第3週の出来事は変わっていない。変わったのは第3週についての画面の言い分）。
  だから跡は**読み手と画面のあいだ**にしか置けない——`初めての読み手` に切り替えると
  既定の跡は **6 → 0 個**になり、対照は **6 → 6 個**のまま出続けます（**その読み手には
  「前の形」が無いのに、変わりましたと言われる**）。跡は時間では消えず（4 秒待って **6 → 6**）、
  `確かめた` でだけ **0** に。旧の形は**常設しません**（跡が出ている状態で線は **1 本**、
  対照は薄い旧線を重ねて **2 本**＝薄さが確度の語彙から奪われる）。旧の形は
  **再演の枠の中でだけ**見え、枠の中の線は本体と `stroke` / `stroke-width` /
  `stroke-dasharray` / `opacity` が**すべて一致**します（**いつの画面かは枠が言う**。No.113）
- **No.130 申告の上に積んだ申告** → 申告が申告を材料にすると、**どこまでが測ったものだったか
  誰も辿れなくなります**。No.125 の伝播は1ホップ、No.127 の長さは平ら——どちらも
  **申告が申告を養う**場合を扱っていませんでした。答えは**台を積む**こと。
  各値の台の下辺の**本数がそのまま段数**で（5状態 × 6行 = **30 チェックで差 0 件**）、
  線種は**二値のまま**（`border-bottom-style` は solid / dashed の **distinct 2 値**、
  太さ **2px**・色 **rgb(61,61,61)**・不透明度 **1** はすべて **distinct 1 値**）
  ——**3つめの線種も、色も、薄さも要りませんでした。** 数字と台の幅は動かず
  （値のテキストは各行 **distinct 1 値**、台の幅は 6行×5状態すべて **130px**）。
  いちばん怖い絵は**測定に着地しない値**で、`来期の粗利` は単価・原価率を両方埋めると
  `[dashed, dashed]` の**全段破線**になります（`売上` は `[solid, dashed]`、
  `粗利` は `[solid, dashed, dashed]` と**最下段が必ず実線**）。
  そして**逆向きの操作でしか担保できない**（No.127 の再演）——`測り直す` で段数は一斉に減り、
  往復すると `data-depth` / `styles` / 値が **JSON 完全一致**します。
  対照（触れたら値ごと破線にして `推定` と名指しする）は、**単価を1回埋めるだけで 4/6 行が
  丸ごと推定**になり閾値（`TAINT_THRESHOLD=3`）を1手で超えてダイアログが割り込みます。
  既定は同じ操作で根拠を失う行が **1 行**だけ。しかも**対照の台の下辺は6行とも常に
  1本・solid の distinct 1 値**——**装飾は増えて、深さの情報はゼロ**です
- **No.131 そろっていない現在** → 8つの指標が並んでいて、**それぞれの取得時刻が違います**。
  鮮度を薄さで言うことは No.117 が封じ、時間の定規は No.128 が x に置いた——
  だから**台を行ごとに持ち、右端をその行の基準時点**にしました
  （期待 x との差は 8 行とも **最大 0.0154px**）。**そろっていないことは、
  台の右端の不揃いで読めます。** 芯は合計で、**合計の台の右端はいちばん古い材料に揃います**
  （`問い合わせ`＝7日前と **差 0.000px**）。だから**1行を新しくしても、合計は新しくなりません**
  ——最古でない `ログイン数` を取り直すとその行の右端は **+152.719px** 伸びるのに、
  合計の右端の差分は **0.000px**。最古の `問い合わせ` を取り直したときだけ合計が
  **+40.781px** 動きます（次点の最古の位置へ）。閾値は1つも持たず
  （`同期済み` / `最新` に相当する要素 **0 個**、台の右端の色・不透明度は **distinct 1 値**）、
  **`全部取り直す` のあとも右端は揃いません**（**distinct 8 値・最大差 59.266px**）
  ——**揃った状態が画面に一度も現れないこと**が、「**揃っているは状態ではなく、
  許容の宣言である**」の証明です。対照は行ごとにバッジを出し、
  ヘッダの `最終更新` と**合計のバッジがどちらも「3分前」＝いちばん新しい行**を名乗ります
  （合計の本当の材料の最古は **7日前**）。`同期済み` は 30 分の閾値をまたぐ
  **1クリックの前後で true → false** に飛びます

この回の収穫は、**「担体の空きは1つの意味しか持てない」の抜け道が、もう1つ見つかった**ことです。
前回 No.127 が「**二値の線種でも、長さを持てば量を運べる**」を見つけましたが、
今回 No.130 は同じ二値の線種で、**本数を持てば深さを運べる**と示しました。
No.124 が候補を**横に並べた個数**（選言）で言ったのに対し、130 は根拠を**縦に積んだ段数**（連言）で言います
——**同じ二値の担体で、並べるか積むかだけが論理を分けます。**

そして**空いていたのは台の枚数だった**、が3種に共通しています。129 は台を
**読み手の側にもう1枚**（跡は「読み手が前に見たかどうか」の関数）、130 は台を**縦に積み**、
131 は台を**行ごとに**持ちました。**台は世界の物ではないので、いくらでも複製できます。**

**実物の目視が拾った誤りは 2件／3種、そのうち数値条件が拾えたものは 0 件**でした（6回連続）。
どちらも**この標本がいちばん見せたいフレームで起きています**。No.130 は区間の間隔を **1px** で
組んでいて、`getBoundingClientRect` は「矩形が重なっていない」を正しく返し C1（本数＝段数）も
通っていたのに、**`border-bottom` は 2px の帯なので 1px の隙間は等倍でほぼ潰れ、
段数2の行が1本の太い線にしか見えませんでした**——**「重なっていないか」は測っていて、
「見分けられる隙間か」は誰も測っていなかった。** 3px に広げ、
**隣接要素の最小間隔を新しい実測項目**にして直しています（全 30 チェックで **3.000px** 一定）。
あわせて、段数0の行は**台そのものが描かれていなかった**ので（担体が無いのか、
担体はあるが空なのかが区別できない）、**全行に薄いレールを常設**しました
——**担体は、それが載る場所が描かれていないと読めない**（この図鑑で何度目かの踏み直し）。
No.129 は再演の枠が **x 軸の週ラベルを囲み切れず**、時間の定規だけが枠の外に落ちていました
（枠の矩形が内側 64 要素を含むかを測って直し、いまは**はみ出し 0 件**）。

**企画の書き間違いも、これまでで一番きつい形で出ました。** No.130 の企画は台本に
`来期の粗利 = 粗利 × 成長率` と書きましたが、そのとおりに実装すると
`来期の粗利 → 粗利 → 売上 → 客数` と辿って**必ず測定に着地してしまい**、
**同じ企画が要求する受け入れ条件 C5（「着地しない値」が存在すること）を満たす行が
1つも作れません**。実装は依存を「客数を経由しない2つの葉だけ」に付け替えて解いています。
**企画の台本と、企画の受け入れ条件が、互いに矛盾していた**のは今回が初めてです。

No.126〜128 は、**画面が自分について何も言っていない**回です。この図鑑の125種はすべて、
**世界の中の何か**（値・行・予告・操作・他人）についての動きでした。画面はずっと語り手として
振る舞い、**自分自身については一度も語っていません**。

| No. | 画面が自分について言っていないこと | 既定の答え |
|---|---|---|
| 126 | **視野**：候補の集合が閉じているという前提 | 台の締め線が外れる。動くものは何も無い |
| 127 | **成分**：その数字が何で出来ているか | 数字は不動。台の下辺が破線と実線の長さで語る |
| 128 | **数え方**：同じ高さが同じ量だという約束 | 目盛りが継ぎ目を横断しない。線は1pxも違わない |

この回を貫いた縛りは、**画面が自分について語るとき、動くのは担体ではなく「台」のほう**、です。
この図鑑の担体（線・塗り・輪郭・点・駒）はどれも世界の中の何かを指しています。
**画面についての事実は、指す相手が世界に居ないので、担体には載りません。**
載る場所は台（レール・トラック・定規）だけです——台は世界の中の物ではなく、
**画面が「ここに何をどう載せるか」を決めた結果**そのものだからです。
系として、3種とも**事実の担体が1pxも動きません**（126 の幹、127 の数字と塗り、128 の折れ線）。

- **No.126 原因が画面に無い** → 3つの施策を全部止めても効果 `+12` が残ります。
  台（レール）は最初から舞台の右端まで伸びていて（4週で最大差分 **0.000px**、右端は舞台の右端と
  差 **0.00px**）、その**候補の右端に締め線が1本**——No.116 と同じ形・同じ意味で
  「この台にはもうこれ以上載らない」。つまり**画面は最初から「候補はこれで全部だ」と
  言い切っていました**。候補が0になった瞬間、その締め線が外れます（**1 → 0 個**）。
  遷移をまたぐ **20 フレーム**で動いた要素は **0 個**、消えた要素は締め線 **1 個**だけ
  ——**読み手が反証したのは効果の帰属ではなく、画面の言い切りのほうです。**
  幹は4週とも `{left:492, width:34}` で不動（最大差分 **0.0000px**、`data-trunk` は常に **1**）で、
  **枝0本＋幹1本が「原因が画面に無い」、枝0本＋幹0本が「何も効いていない」**——
  枝の個数だけでは足りず、**幹と締め線の3つで読みます**。締め線が消えることを No.116 の
  「戻せるようになった」と取り違えないよう、週送りの前後で履歴は **±0**（増えるのは
  トグルを押したときだけ）。対照は `その他` を候補に足して実線を引き（カード **3 → 4**）、
  幹の根元が **0.34s** で乗り換えます——さらに `その他` は**止められて**しまい、
  押すと因果の線が **0 本**になるのに効果は **`+12`** のまま。
  **画面に無いものに名前を付けると、意味のない操作が読み手に提供されます**
- **No.127 ほとんどが申告になる** → 行は増えません。**12行のまま、合計も ¥120,000 のまま**で、
  変わるのは「そのうち何行が測定で、何行が申告か」だけ。合計の文字列は4週とも **distinct 1 値**、
  塗りは最大差分 **0.0000px**。動くのは**台の下辺**で、12行ぶんの区間に分かれ、
  長さは金額に比例し、線種は測定なら `solid`・申告なら `dashed`
  ——**125 の二値の線種のまま、長さで量を運びます。3つめの線種は要りませんでした。**
  `data-declared-px` は **0 → 100.00 → 217.50 → 288.75px**（実測との差はいずれも **0.0000px**）。
  金額の大きい行から先に申告になるので**個数と量が割れます**——
  **週2は個数 3/12（25%）に対して下辺は 33%、週4は個数 83% に対して下辺は 96%。**
  区間どうしは太さ・色・不透明度が **distinct 1 値**で、違うのは線種と長さだけ。
  閾値も警告も色も **0 個**で、**多いかどうかは画面が判定しません**。
  `測り直す` で1行戻すと下辺は **288.75 → 253.75 → 288.75px**（往復差 **0.0000px**、
  その行の金額 ¥14,000 と期待差 **35.00px** が一致）——**台の長さが本当に成分の関数であることは、
  逆向きの操作でしか担保できません。** 対照は `推定` バッジが **0/3/7/10 個**並び、
  半数を超えると塗りが赤く（**2 値**に割れ）`信頼度: 低` が出ますが、
  **対照の台の下辺は全週 `solid` の distinct 1 値**——**装飾は増えて、成分の情報はゼロです**
- **No.128 定義が途中で変わった** → 第7週から数え方が変わります（`7日以内` → `30日以内`、
  週6→7 は **+95.9%**）。**跳ねていません。別のものを数え始めただけです。**
  答えは、**比較可能性の約束が、目盛り線ではなく目盛り線の「横断」に入っていた**という読みから
  出ました。既定では**水平の目盛りが継ぎ目を1本も横断せず**（横断 **0 本**、端点 x は
  `data-seam-x` **152.00** と全部差 **0.00px**）、対照は **4 本**が貫通します。
  一方で**時間は連続している**ので x 軸は1本のまま全幅を貫通し、週ラベルは **12 個**の連番、
  間隔は **distinct 1 値**——**貫通する定規と貫通しない定規を並べて、
  「時間は続いている／数え方は続いていない」を撃ち分けます。**
  そして**既定と対照で折れ線の12頂点が完全に一致します**（最大差分 **0.0000px**、`d` 属性は同一文字列）
  ——**線は何も間違っていません。間違っているのは定規のほうです。**
  同じ区間の2点なら差の帯が出て（`data-delta` **-4** が実測と一致）、
  継ぎ目を跨ぐ2点では帯 **0 個**・**追加要素 0 個**——エラーも「比較できません」も出さず、
  選択は残ります。対照は跨いでも帯を出して**存在しない差を `+96%` と言い切ります**

この回の収穫は、**継承した語彙には、継承先の縛りが禁じている演出が付いてくる**と分かったことです。
No.126 は No.124 の語彙をそのまま継承しましたが、124 は `.is-solid` の跳ねと結果値の登場に
`animation` を持っていて、この回の縛り（既定の全要素で `animation-name: none`）と正面から衝突しました。
**語彙を継承すると決めることは、その語彙が持っている緩急まで継承すると決めることです。**

もう1つは、**台が自分の名前を名乗るのは、名指しではない**ということです。No.128 の企画は
「文言 0 個」と書きましたが、実装は継ぎ目の左右に `7日以内` / `30日以内` を置きました。
これは値についての名指し（No.124 の対照の語彙）ではなく、**定規が自分の単位を名乗っている**
——この回の主題からすれば、**台は画面について語ってよい唯一の担体**なので、正しい判断でした。
同じ線で、**書式そのものが芯になる**ことも分かりました。No.128 の既定の帯は生の差分（`-4`）、
対照は百分率（`+96%`）です——**`%` という書式が「同じ母数で割った」という主張を勝手に持ち込みます。**
企画は帯の中身の書式を指定していませんでしたが、この標本にとって書式は芯そのものでした。

**企画の側の穴も1つはっきりしました。継承元の構造をそのまま引き継ぐと、
継承先で意味を失った要素が残ります。** No.127 の企画は No.125 の語彙（塗り＋輪郭）を
継承する体で書き始めましたが、**127 の合計は常に 100% 既知なので、輪郭に相当する要素が存在しません**。
「幅を持たない」とは書いたのに、その帰結（塗りとトラックを分ける理由が自明でなくなること）を
書いていませんでした。**継承を宣言することと、継承先で何が残って何が消えるかを書くことは別の作業です。**

作って初めて見えたことも1つあります。No.127 で、**個数と量は同じ画面の別の量**でした。
金額の大きい行から先に申告に変わるので、週2は個数では 3/12（25%）なのに下辺ではもう 33%、
週4は個数 83% に対して下辺は 96% です。**バッジは個数しか出せません**——
1行の巨大な申告と10行の小さな申告が同じ絵になる、が対照の本当の壊れ方でした。

目視は今回も1件拾いました（数値条件が拾えたものは **0 件**）。No.128 の対照のバッジで、
位置決めの SVG 属性 `transform` と入場アニメの CSS `transform` が同じ要素に載っていたため、
**アニメ終了後にバッジがカード左上へワープしていました**。座標も個数も条件は全部通っていて、
**絵としてだけ破綻していた**——5回連続で同じ形の誤りが、別の標本・別の実装者の手で出ています。

No.123〜125 は、**答えが手に入ったあと**の回です。No.120〜122 は「画面が答えを持っていない」
ことを描きましたが、**答えが手に入っても、それは素の事実にはなりません**。

| No. | 答えは、どう届かないか | 既定の答え |
|---|---|---|
| 123 | **遅れて**届く | 動くのは**知ったこと**の担体だけ。事実の担体は1pxも動かない |
| 124 | **どれのおかげか言えない**まま届く | 候補は**個数**で言う。実線の幹は常に**1本** |
| 125 | 画面ではなく**読み手**が出す | 減るのは値ではなく**幅**。申告は剥がせる |

この回を貫いた縛りは、**「知った」と「起きた」を、同じ動きで言わない**ことでした。
この図鑑では動きは常に「起きたこと」の語彙なので、遅れて届いた答えを**線を伸ばして**見せると、
ずっと前に起きたことが「いま起きた」になります。だから 123 の線は動かず、
124 の結果側の先端は候補が減っても動かず、125 の塗りは読み手が答えても動きません。
**3種とも、動いてよいのは「いま、その場所で起きたこと」の担体だけ**という1つの規則で書かれています。

- **No.123 あとから答えが来る** → 列①の遅着の前後 20 フレームで、実線の `height` 差分は
  **0.000px**、`data-gap-px` は終始 **18.00**。破線は遅着の後も **1 個**残ります
  ——**分からなかった時間は、消してよい記述ではありません**（あのときの破線は正しかった。
  訂正ではなく**解消**です）。受理番号は**判明した順**なので、並びは列①=`受理 #2` /
  列②=`受理 #1` / 列③=`不受理 #3` ——**左の列の番号のほうが大きい**という乱れそのものが、
  「あとから分かった」と「そのあいだに次を出していた」を、**新しい担体を1つも足さずに**語ります。
  チップ3つは `background-color` / `border-color` / `font-size` / 外形 **68×21px** がすべて
  **distinct 1 値**——**遅れて来たことも、不受理であることも、チップの見た目では言いません**。
  対照は線が **19.750px** 伸び、破線が **0 個**になり、列②のチップが `受理 #1` → `受理 #2` へ
  **書き換わります**（**あとから来た答えが、先に知っていた事実の番号を書き換える**）
- **No.124 どれが効いたのか分からない** → 枝の個数と `data-candidates` は **3 / 2 / 1**、
  幹は常に **1 本**（`width` は3週とも **50px**）。枝は太さ・線種・色・長さ **34px** がすべて
  **distinct 1 値**で、**候補であることしか言わず、寄与の大小は1つも語りません**。
  候補が減っても結果側の先端は **393/188.5/6/6** のまま（切り替えの 20 フレームで最大差分
  **0.0000px**）。そして**候補が1つに絞れた瞬間、その枝だけが `dashed` → `solid` に変わり**、
  絵が **No.65「因果のリレー」と同じ1本の実線**になります——**No.65 は、この標本の特殊解**でした。
  対照は `主要因` バッジが常に **1 個**、カード色が **2 値に割れ**、名指しの元が候補から外れると
  線の根元が **252.37px → 347px** へ**乗り換わります**（`transition-duration` **0.34s**、
  途中のサンプルは **304.83px**）——**起きていない出来事のアニメーションが実際に走ります**
- **No.125 読み手が答えを埋める** → 答えても塗りは **0.000px** しか動かず、輪郭だけが
  **90.00px → 4.00px** に縮みます（`広告費です` は右端、`違います` は左端。
  `border-color` / `border-style` / `opacity` / `width` は完全一致で、**違うのは `left` だけ**）。
  申告の行は `dashed`、測った2行は `solid`、一方で背景・字の大きさ・高さは **distinct 1 値**
  ——**濃さで格を付けません**。合計の担体の縁も**答えても破線のまま**で、
  **申告が1つでも混じった合計は、測った合計ではない**が下流に伝播します。`外す` で輪郭は
  **90 → 4 → 90px** と往復（差 **0.00px**）。対照は塗りが **+90px** 伸びて数字が
  `¥18,000` → `¥27,000` とカウントアップし、3行とも `solid` になって**申告と測定が
  区別できなくなり**、`外す` が **0 個**、答えるまで `次の週へ` が押せません（`dialog` **1 個**）

この回の収穫は、**「決め打ちの台本」は「乱数を使わない」だけでは決まらない**と分かったことです。
No.123 の企画は「すべて定数と提出順で決まる」と書きましたが、遅着タイマーが各提出の時刻を
起点にしている以上、**番号の順は提出の間隔にも依存します**（③を①から 1200ms 以上あけないと
順番が入れ替わる）。しかも同じ企画は「列③は `次へ ▶` のあと提出」とも書いていて、これは
No.122 の `isBlocked` を継承すると**実装不可能な手順**でした（列②は即着地するので `次へ` が出ない）。
**継承する語彙を指定するだけでは足りず、継承先で手順が成立するかは別に確かめる必要があります。**

もう1つは、**受け入れ条件の書き方が、実装の幾何を決めてしまう**ことです。No.124 の C7 は
「対照は幹の根元の **x** が変化する」と書きました。既定と同じ縦並びのままだと名指し対象の変化は
y にしか出ないので、実装は**対照だけ横並びレイアウトにする**判断をしています。
企画はレイアウトを指定したつもりがなく、**測りやすさのために選んだ軸が、対照の姿を規定していました**。

そして No.124 は、この図鑑にとって**既存の標本を一般化する**初めての標本になりました。
候補が1つに絞れたときの絵が No.65「因果のリレー」と一致するので、**No.65 は
「帰属が分かっている場合」という特殊解**として読み直せます。ついでに1つ、作って初めて
見えたことがあります——**因果が確定する瞬間は、効果が起きていない週です。**
原因を止めて効果が消えることでしか特定できないので、リレーの絵が完成するそのフレームで、
結果の欄は **`効果（今週） 0`** を出しています。**確定した因果の線と、今週の測定値は、
別のことを言っている。**

No.120〜122 は、図鑑が **119種すべてで前提していたこと**を壊した回です。
それは **画面は語り手として答えを持っている**、という前提でした。
117〜119 は予告が思いどおりに来ないことを撃ちましたが、あの3種でも画面は
自分の遅れも外れも正確に知っていました。この回の3種は、画面が**知らない**ことを描きます。

| No. | 何について答えが無いか | 既定の答え |
|---|---|---|
| 120 | **これから**のこと（予告の確度） | 境目は閾値ではなく**幾何**で決まる。輪郭は消えるが**点が残る** |
| 121 | **いま**出している事実（画面が古い） | 駒は枠に**一度も入らない**。載せるのは「取れなかった」 |
| 122 | **読み手自身の操作**の結果 | 線が結果の欄に**届かないまま止まる**。スピナーを持たない |

この回を貫いた縛りは1つです。**「知らない」は、着地しない動きで言う。**
この図鑑では動きは常に「起きたこと」の語彙なので、担体を目的地に着地させると
「答えが出た」と読まれます。だから 120 は輪郭が台の端で行き止まりになり、
121 は駒が枠に入れず手前で止まり、122 は線が結果の欄に届きません。
薄さ（＝確度、No.74/114）・消滅（＝やめた、No.114）・色（＝対照の語彙）は
どれも既に埋まっているので、**残っていたのは「着地しないこと」だけ**でした。

- **No.120 もう予告できない** → 外れが積もって輪郭は **4 → 26 → 71 → 136px**（増分が
  直前の |外れ| と**差 0.00px**）と太り、5週目に **244px** で台に収まらなくなります。
  その瞬間、輪郭は**トラック全幅**（`left=0 / width=300`、両端とも差 **0.00px**）＝
  **行き先が無い**。境目を決めているのは定数ではなく**トラックの物理的な端**です。
  引き渡し後は輪郭 **0 個**・点 **4 個**（週を進めると **5 → 6 個**）で、点は
  `opacity` も `background-color` も大きさも **distinct 1 値**——**古さも順番も薄さでは語りません**。
  対照は幅 **20.00px 固定**（増分 **0.00**）で、最後は輪郭ごと消えて**材料が何も残りません**
- **No.121 先に取られていた** → 押してから戻り切るまでの **55 フレーム**で、駒と枠の重なりは
  **最大 0px²・非ゼロ 0 枚**＝**駒は枠に一度も入りません**。**操作は届いた／結果が無かった／
  失敗ではない**の3つが1つの動きで撃ち分けられます。他人の分は `animation-name` **`none`** /
  `transition-duration` **`0s`** で現れる（**出発点が画面の外にあるものは、動きで描けない**）。
  履歴に載るのは「取れなかった」で、「他」を含む要素は **0 個**。対照は駒が枠に完全に入り
  （重なり **196px²** が **38 フレーム**）、行が **1 → 0 個**になり、トーストが他人を主語にします
- **No.122 届いたかどうか分からない** → 成功は線が枠に**接し**（gap **0.00px**）、
  分からないは手前 **18.00px** で止まってそのまま残ります（停止後 25 サンプルで
  `height` は **distinct 1 値・偏差 0.00000px**）。既定は**全要素の `animation-name` が `none`**
  ——スピナーを1つも持ちません。`確かめる` を3回押しても線は **0.000px** しか伸びない。
  対照は失敗と言い切り、`再送` で結果の欄に **`受理 #2` と `受理 #3` の2件**が入ります
  ——**二重に提出されたことが画面に見えます**

この回の収穫は、**受け入れ条件を全部通したまま、絵が空になることがある**と分かったことです。
No.120 は引き渡しの直後——**この標本がいちばん見せたいフレーム**——で、4点すべてが
事実の塗り（`#3d3d3d`）の中に入り、点も同じ色なので**1つも読めませんでした**。
それでも C1〜C8 は全部通っています。C4 は点どうしが同じ見た目かを、C8 は点がトラックの
矩形の中にあるかを見ているだけで、**濃い担体の上で読めるか**は誰も測っていなかったからです。
**担体は、それが載る場所が描かれていないと読めない**の3回目ですが、今回は地の色ではなく
**別の担体が下地になった**という新しい形でした（点に地色の縁を付けて解いています）。

もう1つは、**「ある瞬間に真」と「真であり続ける」は別だ**ということです。
No.122 の初版は提出を縦に直列で積む作りで、2回目が積まれた瞬間に**1回目の線の行き先が
「2回目の点」に変わり**、`受理 #1` が**2回目の破線の真下**に座りました——
「線が届かなかったのに受理が入っている」と読めてしまう。C2（1回目の線は枠に接する）は
**測った瞬間には真だったが、絵として残っていませんでした**。提出ごとに自分の列を持つ
**くし型**に変えて、1回目の gap が最終フレームでも **0.000px** であり続けるようにしています。

企画の側の書き間違いも2つ出ました。No.121 の受け入れ条件は「重なりが枠の全面（24×24）に
達する」と書きましたが、駒は 14×14 なので**幾何的に不可能**です。No.122 は「結果の欄が 0 個」と
書きましたが、「跡は消えない」という原則と**同時には成り立ちません**（1回目の受理が残るため）。
**原則と受け入れ条件を別々に書くと、互いに矛盾していても書けてしまう。**

実装の側では、**数値条件の届かない範囲**が1つ実演されました。No.121 の他人の取り分は、
CSS が親と違う座標系で書かれていて**画面の外まで吹っ飛んでいた**のに、**C1〜C8 は全部通ります**
——C1 が見ているのは「動かないこと」であって「**正しい場所にあること**」ではないからです。
目で見て初めて気づきました。計測の罠も1つ増えています。No.122 では**測る対象を挟む親の
`flex` の `gap`** が幾何を 8px ずらしていました（gap 0.00px のはずが 8.000px と出た）。
既存の教訓は「測る対象に `transition` を付けない」でしたが、**親のレイアウトも測定値に混ざります**。

No.117〜119 は、No.114〜116 が**検証せずに前提していた3つ**を1つずつ壊した回です。
あの回は「まだ起きていないこと」をよく撃ちましたが、予告が**すぐ出せて・当たって・
閉じるのは読み手**であることを、どれも疑っていませんでした。

| No. | 壊した前提 | 既定の答え |
|---|---|---|
| 117 | 予告は**すぐ出せる** | 待機で**消さず・動かさない**。鮮度は**ズレの長さ**で言う |
| 118 | 予告は**当たる** | 外れを名乗らない。外れは**次の予告の幅**になる |
| 119 | 閉じるのは**読み手** | 起きなかったことは動かせない。動くのは**現在地**だけ |

この回を貫いた縛りは企画の段階で見つかりました。**No.114 は「輪郭が消える」に "やめた" の意味を
割り当てている**ので、この3種は**消えることで何かを言えません**。117 は「待っている」を消さずに、
118 は「外れた」を消さずに言い、119 は操作そのものが無いので**別の担体**を探すことになりました。
**担体の空きは、1つの意味しか持てない。**

そして 119 で、**担体を置く相手が「出来事」の側にも居ないことがある**と分かりました。
No.116 は不可逆を「履歴の点が増えないこと」で言えましたが、**何もしなかった未来には
対応する出来事が無い**ので、増えない点すら置けません。答えは担体を**履歴から週の定規へ移す**こと——
**読み手がやったことは履歴に、時間がやったことは定規に**。台帳が違います。

- **No.117 届いた予告はもう古い** → 待機中の輪郭は **0 個のフレームが 0 枚**で、事実の塗りは
  **distinct 1 値・偏差 0.0000px**。鮮度は**係留線の長さ**（`data-lag-px`）で言うので、輪郭の
  `opacity` は待機中も着地後も **distinct 1 値**＝薄さは1度も鮮度を語りません。古い応答は
  着地させないので `data-preview-for` の**後戻りは 0 回**。対照（届いた順に差し替える）は
  同じ操作で**後戻り 18 回 / 54 着地**——つまみは前へ進んでいるのに予告が後ろへ戻ります。
  さらに対照は**手を離したあとも保留中の取り寄せが届いて動き続ける**（離してから 700ms で
  到着ログが 33 → 42 件）。**古さを名乗らない実装は、静止するのではなく、いつまでも遅れて動き続ける**
- **No.118 予告どおりに来なかった** → 既定は着地後も輪郭が**残り**（消えるフレーム **0 枚**）、
  塗りの右端との差 **44.00px** がそのまま画面に残ります（`data-miss-px` と**差 0.00px**）。
  外れは次の予告の幅になり、輪郭は **4px → 26px → 48px**（増分 **+22 / +22**）。
  下振れと上振れで跡の色・不透明度・線種は**完全一致**し、増分も**同値**。対照は3週とも **20px**
  （何回外れても次の予告の見え方が変わらない）で、**下振れの週だけ**赤くなり、
  同じ大きさの上振れには「予告どおりでした」と言ってしまう＝**確度の見積もりが片側にしか育たない**
- **No.119 何もしなかったことで閉じる** → 失効の瞬間、機会の区間は `left` / `width` / `opacity` の
  差分が**すべて 0.000**。同じ窓で動いた要素は**現在地の縦線 1 個だけ**（**41.19px**）。
  履歴の点は **±0**・締め線 **0 個**（No.116 との撃ち分け。取ったときだけ **+1**）。
  残り時間に担体は足さないので、閉じる **3週前 / 1週前 / 0週前**で区間の見た目は**完全一致**——
  期限を言っているのは**右端と現在地の距離だけ**です。対照は1週前に点滅アニメが付き、
  失効でカードが消え、トーストは 1800ms で消えて**閉じたことが画面のどこにも残りません**

この回の設計上の収穫は、**「畳む」を、閉じた瞬間から切り離せると分かった**ことです。
跡は畳んでよい（No.91）が、**畳みを失効と同じ操作でやると、動かないはずの担体が動きます**。
119 の実装は畳みの発火を**失効の次の週送り**に置きました（`week - end >= 2`）。おかげで
失効直後の1週間は**過ぎているのに何も畳まれていない**空白が残り、その1週間だけは
**縦線が右端より右にある**という位置関係だけが「過ぎた」を言います。
**畳みは、閉じたことを知らせる合図ではなく、後からの整理である。**

正直に書いておくべきことが1つあります。119 で「もう取れない」を言っているのは、区間ではなく
**ボタンのほう**です（`取る` は週が過ぎた瞬間に `disabled` になる）。この標本の主張は
「機会の担体を動かさない」ことであって「何も変わらない」ことではありません。
**閉じたことは操作の側が言い、閉じた経緯は定規が言う。**

実装の側では、**前回と同じ失敗が、別の標本で、別の実装者の手で再現しました**。No.118 は数値条件を
全部通したまま、**トラック（レール）が舞台の地色と同色で実質見えず**、塗りと輪郭が余白に浮いて
「これは何かを測っている場だ」が伝わらない絵になっていました。前回 No.116 で敷いたレールと同じ直しです。
**担体は、それが載る場所が描かれていないと読めない**——1回の発見では足りず、標本ごとに踏み直しています。

CSS の側の罠も1つ増えました。No.118 の輪郭は4辺に `border: 1.5px dashed` を持つ箱なので、
**`width: 0` を指定しても content box を負にできず、実際の描画幅が border のぶん広がります**
（輪郭の右端が 2px ずれ、実測差が **46px 対 44px** で一度落ちた）。No.114 が同じ罠を
「左辺だけの線」に逃げて解いたのに対し、こちらは**箱のまま最小幅 4px**で解きました。

企画の側の誤りも1つ出ています。No.118 の既定は当初、**外れを文章で名乗っていました**
（`確定は成功。ただし予告どおりには来ませんでした`）。原則の1つめが「外れをUIが名乗らない」
なのに、実装がそれを文章で言ってしまい、企画（配線側）がレビューで直しています。
いまは的中でも外れでも **`確定しました`** で同一——**違いは輪郭と塗りの位置関係にしか出ません**。
**動きで言うと決めた主張は、文章を1行足すだけで無効になる。**

No.114〜116 は、No.111〜113 が開けた穴の続きです。あの回は「動きが語る時刻」を3通りに撃ちましたが、
**3つとも過去と現在の話**でした（引き延ばす・巻き戻す・再演する）。残っている向きは1つ——**未来**。

| No. | 未来の描き方 | 既定の答え |
|---|---|---|
| 114 | **1つの未来**を先に見せる（予告） | 事実の担体を動かさない。予告に緩急を付けない |
| 115 | **2つの未来**を並べて比べる | 同じ形・同じ濃さ・同じ原点。分けるのは名前だけ |
| 116 | **未来が閉じる**（戻せない操作） | 履歴の担体が**増えないこと**で言う |

共通の設計則は **「起きていないことは、起きたことと同じ担体では描けない。
弱めるのでもなく（薄く・遅く・小さく）、別の担体に移す」**。

そしてこの回で、**No.113 の答えがそのままでは効かない**場所が見つかりました。

- **再演は、動きを1msも変えないのが正解**（本当に起きたことなので、同じものを見せる正当性がある）
- **予告は、動きそのものを見せないのが正解**（まだ起きていないので、動かした時点で「起きた」になる）

**同じ「いつの出来事か」の問題でも、過去と未来では答えが逆になる。**
過去は動きを保存して枠で語り、未来は動きを捨てて位置だけを語る。

- **No.114 まだ起きていないほうの動き** → つまみを握るあいだ事実の塗りは **distinct 1値・偏差 0.0px**
  （対照は **25 サンプル全部が別の値・偏差 119.06px**）。予告の輪郭には `width` の transition を
  **そもそも定義しない**（computed `0s`）ので、5回のジャンプに対し位置は **distinct 5個・中間値 0個**＝
  **中割りが1枚も無い**（対照は塗りに **12 枚**）。確度は薄さではなく**幅**で言うので、3本の `opacity` は
  フェードの中間でも **0.37 = 0.37 = 0.37** と一致し、違うのは幅だけ（**2px / 2px / 68.875px**）。
  **やめた**ときは輪郭が幅を変えずに消え（**偏差 0.000px**）、**確定した**ときは輪郭が消えないまま
  塗りが中を満たす（輪郭が消えるフレーム **0 枚**、追いつき後の右端の差 **0.00px**）
- **No.115 二つの予告を並べる** → 既定は予告が **2個**同時に立ち、対照は**最大 1個**。対照で A→B と
  ホバーを移すと予告 **0 個のフレームが 2 枚**挟まる＝**比較が途切れる**（既定は **0 枚**）。
  2つの輪郭は `border-style` / `opacity` / `border-width` / `border-color` まで**完全に一致**し、
  原点も揃う（`left` **144px / 144px**）。選ばれなかった未来は**塗りが伸び切ってから**退場し
  （着地から **2.4ms**）、そのあいだ幅は **24px 固定・偏差 0.0px**、`opacity` は **1 のまま変化量 0**
- **No.116 戻せない操作** → 既定の2つのボタンは `background-color` / `font-size` / `166×41px` まで
  **完全に一致**する。**差は履歴の列にしか出ない**——可逆は点が **+1**、不可逆は **±0**。
  押しているあいだは締め線が引かれ、外へずらして離すと引き戻る（`data-week` の変化 **0**）が、
  **長押しは要求しない**（**30ms / 60ms / 300ms** のいずれでも確定）。確定後は点が増えないまま
  それまでの点も辿れなくなり、締め線だけが **1 個**残る。対照は **2 クリック**と `role="dialog"` を要し、
  確定後の列に締め線は **0 個**＝**戻れないことが画面のどこにも残らない**

この回の設計上の収穫は、**過去と未来で答えが逆になる**と分かったことです（上記）。
あわせて、No.114 が予告の担体（破線の輪郭）を作り、115 がそれを**2つに増やし**、
116 がそれを**履歴の担体の未来**に使う——**1つの担体を3回、別の場所で試した回**にもなりました。

実装の側では、**主張を壊す経路が今回も2つ**出ています。ひとつは No.114 で、
**確定ボタンへ向かう動きそのものが、確定される値を書き換えていました**。ネイティブの
`<input type="range">` はドラッグ中ポインタをキャプチャしたまま、Y が台を外れて確定ボタンへ
向かっていても **X だけから value を更新し続ける**——実測で **71 → 50** へ読み手に見えないまま
書き換わります。受け入れ条件（輪郭が消えないか・残るか）は構造を見ているので**通っていました**。
**通っていたのに、「確定は今見ている値のはず」という主張そのものが壊れていた**（PR #32 の No.111 と同型）。
もうひとつは No.116 で、数値条件は正しく通っているのに、**履歴の列が空のときは担体が地の色に溶けて
画面から読めません**でした。点が3個あれば読める——つまり**担体が読めるかどうかが、列に既に点が
あるかどうかに依存していた**。列に**レール**を敷いて解決しています。
**担体は、それが載る場所が描かれていないと読めない**（No.104 の「空間を描かないと空間についての
主張は動きにならない」の、1次元での再演）。

No.115 は**起点のズレ**という罠を1つ足しました。「塗りの尺ぶん `setTimeout` で待ってから退場する」は、
`setTimeout` が**クリックの時刻**から数えるのに対し CSS の transition は**次のペイント**から走り出す
（実測 40〜50ms のズレ）ため、**退場が着地より先に来ることがあります**。直しは
**塗り要素自身の `transitionend` を起点にする**こと——**「伸び切った」を時間の見積もりからではなく、
ブラウザが実際にそう言った瞬間から取る**（修正後は常に 2〜3ms）。

収録についても1つ増えました。No.114 の確定ジェスチャーは **X を変えずに真下へ**切らないと、
運んでいる最中に値がドリフトします。No.107 以来「主題が写るかどうかは撮る位置で決まる」と
言ってきましたが、ここでは**撮り方が主張そのものを壊す側に回りました**。

No.108〜110 は、No.90〜107 が**担体を置く相手（行）はいつでもそこに在る**と暗に置いていたことを撃った回です。
No.106 は「現在地が**枠の外**にある」を扱いましたが、あれは**行そのものは描かれていました**（枠を動かせば見える）。
この3種では、行が画面に**居ません**。居ない理由が3通りあり、**誰の都合か**がそれぞれ違います。

| No. | 相手が居ない理由 | 誰の都合か | 代わりに何を言うか |
|---|---|---|---|
| 108 | 行は在るが**描かれない**（親が畳まれている） | 読み手 | 畳んだ親が代弁する。**段数**（`2段内`） |
| 109 | 行が**まだ生まれていない**（末尾が伸び続ける） | 台帳 | 末尾は行ではなく**状態**（`● LIVE`） |
| 110 | 行は在るが**まだ手元に無い**（取り寄せ中） | 機械 | 行番号だけ名乗り、**位置は名乗らない** |

共通の設計則は、**担体が指す相手が居ないとき、代わりに置く担体は「現在地」ではなく別の事実を言っている。
だから形を変える。同じ形の濃さ違いにすると、読み手はそれを弱い現在地として読む**こと。
No.95「担体は1つの事実しか言えない」を、**相手が不在の側**から3回踏み直した回になりました。

- **No.108 畳まれた中** → No.106 の答え（縁に方角）が**使えません**。畳まれた中は上でも下でもなく「内側」で、方角の指す先が無いからです。代弁は親の行に、**囲みとは別の形**で置く（左の縦棒＋段数のタグ）。実測で開閉 30 サンプル中**両方が1個以上のフレームは 0 枚**。対照は親に同じ囲みを出し、そのとき現在地は `i112 → s11` と**書き換わって開き直しても戻りません**＝畳んだだけで現在地を失う
- **No.109 逃げ続ける末尾** → **「末尾に居る」は行 id で持てません**（1件増えるたび古くなる）。既定はライブ中に囲みを **0個**にして枠の右下の `● LIVE` だけで言い、対照は最終行に囲みが立って **3秒で `data-row` が4回**変わります＝**行が増えるたび現在地が飛び移る**。芯は**「追いついた」と「未読がある」は別の事実**であること——読み飛ばして追いつくと、既定は戻り道 **0個**なのに未読は **3件**残ります（対照は同じ操作で **3→0**、読み飛ばしの記憶が消える）
- **No.110 まだ手元に無い** → 素朴な答え「推定の位置に囲みを置いて、届いたら直す」は、No.79「楽観のあと出し訂正」を**現在地で**やることです。対照は届いた瞬間に囲みが **26px 跳び**、既定は同じ瞬間に可視行の `offsetTop` が **1つも変わりません**。だから既定の答えは、**位置が分からないなら位置を名乗らない**——取り寄せ中の担体は枠の外に固定して（枠内 y は 20 サンプルで **286px の1値のみ**）行番号だけを名乗ります。`囲み` / `方角` / `取り寄せ中` の3つは、送る→届く→押して飛ぶ→失敗→再取得 まで一続きに走らせた **65 サンプルで合計が常にちょうど 1**

この回の設計上の収穫は、**「無い」の描き方には3つ別々の答えが要る**と分かったことです。
No.106 の答え（縁に方角を出す）は**枠の外**にしか効きません。畳まれた中には方角が無いので**段数**で言い、
まだ生まれていない末尾には行が無いので**状態**で言い、まだ手元に無い行には位置が無いので**行番号だけ**を言う。
共通するのは、**代わりに置く担体は「弱い現在地」ではなく、別の事実を言っている**ということです。

実装の側からは、同じ穴が**この回だけで2回**出ました。No.108 は「開いたあとの行位置を先に計算してから
畳み状態を更新しないと、まだ畳まれたままの `scrollHeight` にクランプされて着地に失敗する」。
No.110 は「現在地を送った直後に `scrollTop` を同期的に書くと、入れ物の高さがまだ古いレンダーの値なので
古い `scrollHeight` に丸められて飛び先に届かない」。
**状態を変える順序が、見た目の正しさを決める。**

No.109 は No.105 が見つけた穴の**別の顔**を出しました。追従判定をネイティブの `scroll` イベントに書くと、
**末尾への自動貼り付け（自分の `scrollTop` 書き戻し）が同じ `scroll` を誘発**し、
誰も触っていないのに追従が外れます。No.105 の「旗の立てっぱなし」と合わせて、
**「ユーザーが操作したのか」をイベントの側で判定する実装は、必ず自分の書き戻しに汚される**。
判定は**手動操作の唯一の入口の中**に書く、というのがこの図鑑の答えになりました。

収録については、No.107 の「主題が写るかどうかは撮る位置で決まる」に**時間の側**が加わりました。
No.109 は未読が可視行数（5）を超えるまで**5秒以上待たない**と、追いついた時点で全部見えてしまい
「追いついたのに未読がある」が写りません。No.110 では、対照の入れ物を現在地ちょうどで打ち止めにすると
囲みが入れ物の末尾に貼り付き、**26px の跳びが枠の下へはみ出して写らない**ことが分かって、
1画面ぶんの余りを足しています。**壊れ方は実測できても、写らなければレビューには届かない。**

No.111〜113 は、担体の話から一歩ずれて、**動きが黙って主張してしまう時刻**を撃った回です。
No.90〜110 の21種はどれも「何が・どこにあるか」を語る担体の話でしたが、動きはもう1つ、
**「それはいつ起きたか」を、言わなくても主張します**。

| No. | 事実の時刻と、動きが語る時刻のずれ | 既定の答え |
|---|---|---|
| 111 | **同時に**確定した17項目を、順に見せる（引き延ばす） | 順番は因果で決める。総尺は件数に依らず固定 |
| 112 | **過去の**状態へ戻る（巻き戻す） | 着地は尺ゼロ。履歴の移動は台帳に書かない |
| 113 | **過去に**起きたことを、もう一度流す（再演する） | 動きは1msも変えず、枠が「いつ」を言う |

共通の設計則は **「ずれを、動きそのもので語らない」**。
遅くする・薄くする・跳ねを削る、で「これは昔の話です」を言おうとすると、
読み手はそれを**弱い変化**と読みます（No.95「担体は1つの事実しか言えない」の、時間の側での再演）。
**ずれは、動きの外側——枠・帯・間——で言う。**

- **No.111 一度に20個変わる** → 順序は事実の側に無く、あるのは**因果**だけ。見た目の都合（変化量順）で
  並べると読み手は**因果を誤って学習**します。実測で既定の順序違反は **0**、対照は **42**。
  いちばん大きく動く `評価額 +38%`（最下流）の確定順位は既定 **17位/17**、対照 **1位/17**——
  **対照では結果がいちばん先に動く**。総尺は既定が17項目 **1352ms** / 6項目 **1278ms**（差 **74ms**）、
  対照は **3301ms** / **1200ms** と件数比例。**段の切れ目の 189〜261ms の「間」だけが段を語り**
  （段内は 48〜64ms、中間帯は **0件**）、確定の拍は変化の有無に関わらず**全17項目**に打ちます
  （既定 `is-settled` **17個** / 対照 **5個**、対照では12項目が「変わらなかった」のか
  「まだ来ていない」のか読めない）
- **No.112 履歴の中の現在地** → 担体を置く相手が居ない**4つめの理由**は、**居ないのが行ではなく台帳のほう**。
  履歴は現在地を持つ立派な台帳なのに画面に描かれていません。既定は枠の外に点の列で描き、
  **戻るの着地は尺ゼロ**（30 サンプルで位置は **3値**＝中割り 0枚。対照は **14個**の中間位置を通り
  移動距離 **460px**）。捨てた分岐は **0.24s** で折れて消え、`進む ▶` は**クリックと同じフレームで**無効に
  （対照は有効のままの**死んだボタン**）。消えた行に戻ると、既定は `data-place` が**消えた行の id のまま**で
  囲み **0個** / 欠けた点 **1個**、対照は **14→9** と**黙って隣の行に書き換わる**
- **No.113 もう一度見せて** → 再演は**実演とまったく同じ動きでなければ確認になりません**。
  既定の実演と再演は `transition-duration` `0.3s`/`0.3s`・`opacity` `1`/`1` で**完全に一致**
  （実装は分岐で揃えているのではなく、**再演かどうかを一度も見ない**）。対照は `0.6s`/`0.5` で尺が2倍。
  再演中も `いまの合計` は**1値のまま**（対照は **2値**＝過去への書き込み）。
  再演中に本物が届いたら既定は **63ms** で再演を切り、対照は **1341ms** 古い値を見せ続けます

この回の設計上の収穫は、**動きの順番・間・尺そのものが主張になる**と分かったことです。
No.111 の「順番＝因果」、No.112 の「尺ゼロ＝これは移動ではない」、No.113 の「同じ尺＝同じ出来事」は、
どれも**担体を1つも増やさずに**意味を変えています。図鑑がここまで積んできた「担体で撃ち分ける」の隣に、
**時間で撃ち分ける**という語彙が並びました。

実装の側では、**受け入れ条件を数で書くことの限界**が出ました。No.111 の「未見」は初版で
**最後まで見ても追い越しても同じ 17件**になっていて、担体が「どこまで見たか」を何も語っていないのに、
条件（8件以上残る）は通っていました。**通っていたのに、通り方が間違っている。**
実物を触って初めて出ています。No.112 では欠けた点のクラス名が仕様と1文字違い、
PR #31 が踏んだのと同じ「名前が違うと条件そのものが測れない」を再演しました。

No.105〜107 は、No.90〜104 の15種が**口に出していなかった2つの前提**を撃った回です。
その2つは「**現在地を動かすのは読み手**」と「**現在地は見えている**」。
前者は主語が1人であることを、後者は現在地とそれを見る枠が同じものであることを暗に置いていました。

| No. | 外れるもの | 何が2つになるか |
|---|---|---|
| 105 | 動かしているのが読み手ではない | **主語**が2人（機械と読み手） |
| 106 | 現在地が枠の外にある | **事実**が2つ（現在地と、現在地の方角） |
| 107 | 枠が2つある | **窓**が2つ（現在地は1つのまま） |

共通の設計則は、**現在地は台帳のもの、見え方は枠のもの、動かす権利は主語のもの。
この3つを1つの担体・1つの状態に畳むと、必ずどれかが嘘になる**こと。

- **No.105 主語が2人** → 機械の現在地は**塗り**、読み手の現在地は**囲み**。実測で既定は別の行に2つ立ち、対照は担体を兼ねて読み手の選択を **768ms** で上書きします。そして**この標本だけは図鑑の基本イージング（ぷるん）を使ってはいけない**——塗りは時間そのものを写した担体なので、緩急を付けると時刻が嘘になります（線形性の残差 既定 **0.93%** / 対照 **64.7%**）
- **No.106 枠の外** → 端に寄せた印は現在地ではなく**方角**という別の事実。現在地は動いていない（読みかけの `offsetTop` は 40 サンプル全部で **408px の1値のみ**）ので、**担体は縦位置を1pxも動かさず、距離は数で名乗る**（枠内 y の偏差 **0.0px**、変わるのは `2行上→8行上` の文字だけ）
- **No.107 窓が2つ** → 囲みは**両方の窓に出る**（同じ事実の2つの像）。追いかけるのは頼まれた窓だけで、もう片方は **280→280** と1pxも動きません。そして**同じ瞬間に、2つの窓が同じ現在地について違うことを言ってよい**（窓Aは囲み、窓Bは `▼1行下`）——見え方は窓の事実だからです

この回いちばんの収穫は **No.106 が出した「動かすな」** です。モーション図鑑がここで出す答えは、
**動いていないものについては動きで語らない**こと。枠外に出た現在地について担体を動かして語ると、
読み手は「現在地が動いた」と読みます。動かしてよいのは枠を越える瞬間の受け渡し 140ms だけで、
その間も縦位置の偏差は **0.0px**（動くのは scale と opacity だけ）。
あわせて、閾値についての一般則も出ました——**閾値を持つなら、その閾値が読み手に見えるものであること**。
「行が完全に見えなくなったら」という1つの閾値は、実測でも行の下端 442px と 1px の狂いなく一致し、
しかも読み手は「見えなくなった」を自分の目で確認できます。

No.105 は実装の側から**状態遷移のバグが2段で出る**ことを教えました。「自分の操作か読み手の操作か」を
旗で持つと、まず旗を立て忘れて**誰も触っていないのに追従が外れ**、それを直すと今度は
`scrollTop` が既に 0 のときブラウザが `scroll` を発火せず、**立てた旗が消費されないまま
次の本物のスクロールを飲み込みます**（＝追従が二度と外れない）。
**旗で操作の出どころを持つ実装は、旗の立てっぱなしという第2の状態を必ず生む。**

No.107 は**収録の作法**を1つ足しました。`↓` を11回押した位置では2つの窓の `scrollTop` が偶然一致し、
**既定なのに対照と見分けのつかないコマ**ができます。9回で「両窓に囲み」、13回で「食い違い」に
なることを実測してから台本を書きました。**主題が写るかどうかは、実装ではなく撮る位置で決まる。**

No.102〜104 は、No.90〜101 の12種が**ずっと1つの前提の上に立っていた**ことを撃った回です。
その前提は「**現在地は行に載っている**」。No.97 が「座標ではなく行の同一性で持て」と言えたのは
台帳に**行という不変量**があったからで、No.100 が器の変化を吸収できたのも行 id が動かないから、
No.101 が「持っているが適用しない」と言えたのも行が時間で壊れないからでした。
**この前提には、3つの別々の外し方があります。**

| No. | 外し方 | 現在地の姿 |
|---|---|---|
| 102 | まだ何も指していない | **0個** |
| 103 | 指しているのが1点ではない | **幅がある（端が2つ）** |
| 104 | 指す先に行が無い | **担体が行でない（連続空間）** |

- **No.102 個数が0** → 「無い」は担体の不在では描けない（読み込み中・空の台帳と**同じ絵に潰れる**）。**名乗らせる**。そして出発地の無い最初の1つは移動ではないので、**湧かせる**（出現と移動を撃ち分ける）
- **No.103 個数が1でない** → 塗り1つでは「範囲」しか言えず「どちらの端が動くか」が黙る。**担体を役割で分ける**。範囲は端点ではなく**集合**で持つ
- **No.104 行が無い** → 座標は No.97 が撃ったもの。**近傍の名前＋相対**で持ち、倍率で**基準が持ち替わる**ことを黙らない。倍率の不動点は**読み手が指した点**

共通の設計則は、**現在地を1つの値で持てたのは行という不変量に寄りかかっていたから。
個数が1でない／不変量が無い場面では、現在地は「値」ではなく「名前のあるものとの関係」で持ち、
担体は役割を分けて描く**こと。1つの担体に2つの事実を兼ねさせると、必ずどちらかが黙ります。

この回でいちばんの収穫は No.103 から出ました。**「範囲を端点で持つ」は、識別子を捨てた実装ではない。**
対照も `topId` / `bottomId` という**識別子**を持っていて、コードだけ読むと No.97 の答えを守って見えます。
壊れるのは、絞り込みの瞬間に下端を「絞り込み後の並びで同じ位置にいる行」へ**解決し直す**その1回だけ。
実測で選択は `[4,5,6] → [4,5,6,7,8,9]` に増え、**解除しても戻りません**（既定は `[4,5,6]` に完全一致で戻る）。
**露骨に位置で持つ間違いより、正しく見える間違いのほうが遠くまで生き延びます。**

No.104 は、**空間そのものを描かないと空間についての主張が動きにならない**ことを教えました。
倍率を上げた画面は白地になり、「指した点が動かない」（既定 **0.0px**）と
「中心を不動点にすると流れる」（対照 **62.94px**）の差が、数値では出るのに**目には映りません**。
世界に固定した薄いグリッドを敷いて初めて、不動点が動きとして読めるようになりました。

No.96〜98 は、No.90〜95 が現在地を**最後は座標で担保していた**ことを撃った回です。
No.93 は席（52px）を残して座標を守り、No.94 は出発地の `scrollTop` を誤差0で復元し、
No.95 は行の上に3つの担体を重ねました。**座標が現在地の代わりにならない場面が3つあります。**

- **No.96 座標は無事なのに、行が結果の外にある** → 可逆な不在に**席は残さない**（残すと絞り込んだ意味が消える）。可逆性は行ではなく**条件の側**に載り、守れる座標が無いときは黙って0にせず「外に出た」と言う
- **No.97 座標に誤差0で戻れるのに、そこに居るのは別の行** → 戻り道が保存するのは座標ではなく**行**。台帳が変わったら、押される前に帯が**劣化を名乗る**
- **No.98 座標としては描けるのに、誰のものかが抜けている** → 他人は**行の外**に、**遅れて**届く。他人はどれだけ動いても**主役にならない**

3種に共通する設計則は、**現在地は「行の同一性」と「戻る手段」で持ち、座標はその写像でしかない**こと。

No.97 でこの回いちばんの収穫が出ました。対照（＝No.94 の実装のまま）で「元の位置へ戻る」を押すと、
`scrollTop` は 238→238 で**画面が1pxも動きません**。それでも着地行は id=12 で、出発した id=9 ではない。
**「戻ったのに画面が動かない」ことが、誤った場所に居続けていることの最も強い証拠になる。**
既定は逆に `scrollTop` が保存値と -102px ずれ、着地行 id が一致します——**誤差が出るほうが正しさの証拠**です。
数値としては完璧に成功し、体験としては完全に失敗する。**正確であることと、正しいことは別。**

No.96 は企画の誤りを1つ暴きました。初稿の台帳（12行）では、対照の静止後の差が **0.0px** になります。
絞り込みで `scrollHeight` が縮んで `maxScrollTop` が下がり、対照が据え置いた値が
**ブラウザの自動クランプで丸められて、既定の同期補正が出す値と偶然一致する**——
対照は「座標を守らない」を実演しているのに、静止後の数値だけを見ると守れているように見えた
（遷移中は最大 29.75px ずれていました）。**静止後の実測が偶然一致することがある。**
No.93 の収穫（静止後は同じでも、動かすと同じではない）の第2形で、あちらは実装の選択で潰せましたが、
こちらは**台帳の設計でしか潰せません**。16行に組み直したところ、対照の差は -68.0px として現れました。
受け入れ条件の書き方にも直しが要り、以後この種の条件は**静止後と遷移中の最大偏差の両方**で書きます。

実装から出た詰まりどころも1つ。行を削除すると `scrollHeight` が縮み、削除前の `scrollTop` が
新しい上限を超えていると、**ブラウザは `useLayoutEffect` より先に `scrollTop` を自動でクランプします**。
だから「現在値から引く」相対補正はクランプと二重に効く。**縮む入れ物の中では、相対補正は自分がいつ効くかを知らない。**

No.93〜95 は、その No.90〜92 が**黙って置いていた前提**を1つずつ崩した回です。
現在地を設計の対象にした時点で、3つのことを何も言わずに仮定していました——
現在地のものは**在り続ける**、現在地を動かすのは**自分だけ**、現在地は**1つ**。3つとも外れます。

- **No.93 現在地のものが消える** → 中身だけ消えて**席は残る**。席を残さなければならないのは「読んでいる行より上」で消えたときだけで、詰まるのは時間ではなく触れたとき
- **No.94 現在地を動かすのが自分ではない** → 移動は**尺ゼロ**。長距離の滑らかなスクロールは経路を運んでいるふりをして何も運ばない。運べるのは出発地に置いた印だけ
- **No.95 現在地が1つではない** → **囲む・塗る・指す**で担体を分ける。ただし強弱を変えてよいのは「いま操作している手」に属する現在地だけで、状態に属する選択は触り方に依存してはいけない

3種に共通する設計則は、**2種類あるものを同じ見た目にしない**こと。
上で消えたのと下で消えたの、頼まれた移動と勝手な移動、手に属する現在地と状態に属する現在地、
そして**「弱い」と「無い」**——どれも、同じ担体で描いた瞬間に読み手が事実を取り違えます。

No.93 でこの回いちばんの収穫が出ました。席を閉じるときの scrollTop 補正は、
「同じ刻みで同期させる」でも「閉じ終わってから一括で引く」でも、
**静止後の実測値はどちらも 0.0px** になります。ところが一括方式は 240ms のあいだ
読みかけの行がいったん浮いて、閉じきった瞬間に戻る——この標本がまさに否定したい体験を
微視的に再現してしまう。**静止画の実測では区別が付かない2つの実装が、動かすと同じではない。**
No.90 の「実測できることと、見て分かることは別」のちょうど裏返しにあたります
（あちらは実測が通っても見えなかった、こちらは実測が通っても見え方が違った）。

さらにその跳ねは、20ms 間隔のポーリングでは1〜2フレームぶん踏み越えられて見えず、
毎フレーム記録するレコーダーに切り替えて初めて出ました。**実測の粒度そのものが、何が見つかるかを決める。**

No.94 は企画を3箇所直しています。しおりの対象項目を「上端にいる」と「中央にいる」で
取り違えていたこと、それに連動する文言の数字、そして「エラー色は既存の警告色に揃える」と
書いたのに**既存92種に彩度を持つ警告色が1つも無かった**こと。
**「既存に揃えろ」という指示は、既存を数えてからでないと書けない。**
受け入れ条件そのものにも誤りがあり、対照の尺を「400ms以上」と先読みで固定していましたが、
実測は330〜360ms——これはブラウザが移動距離から決める値で、**実装が握っていない数値でした**。

No.95 では、仕様どおりに書いたつもりのコードが仕様の禁則を踏んでいた例が出ました。
行は `tabIndex` を持たないので祖先へフォーカスは移らない、という読みが誤りで、
ブラウザはクリックされた要素から最も近いフォーカス可能な祖先へ既定でフォーカスを移します。
結果、**マウスでクリックしただけなのに主役がキーボード側へ倒れていた**。
輪郭の太さが 1px → 2px に変わるのを測って初めて分かりました。

No.90〜92 は**読み手の現在地**を3つ集めた回です。
89種はどれも「見ている人の居場所は動かない」ことを黙って仮定していました。
現在地は3通りに壊れます——外から来たものにずらされる、遠くに溜まったものが読めなくなる、
現在地そのものが移動する。

- **No.90 外から届くものにずらされる** → 読んでいる行は**1pxも動かさない**。増えたことは縁の気配が言い、気配は消えずに行へ渡る
- **No.91 遠くに跡が溜まって読めなくなる** → 束ねずに**畳む**。落としてよいのは詳しさだけで、件数と位置は最後まで落とさない
- **No.92 現在地そのものが動く** → 飛べるのは**隣まで**。連打中は一度も飛ばず、いまどこに居るかは動きの完了を待たずに読める

3種に共通する設計則は、**現在地は動きの完了を待たずに読める**こと。
動きが足してよいのは「どこから来たか」「どれが新しいか」という関係だけで、状態そのものを動きに預けない。
そして減らすには下限があります——薄めてよいが消してはいけない（消すと「読んだ」「無い」という嘘になる）。

No.90 は企画を一度作り直しました。実測（到着前後で 0.0px、対照は1件あたり 52.0px）は初稿から
満たしていたのに、行が全部同じ見た目だったせいで、画面を見ると対照は「1行ぶんスクロールした」
にしか見えなかった。**実測できることと、見て分かることは別**で、失った現在地を目で読むには
読み手が見ていた1行（読みかけの印）と、動かない基準（物差しの破線）の2つが要ります。

No.92 も企画の数値を1つ直しています。距離の閾値を「格子の中心から」で決めていたのが誤りで、
コードが実際に見ているのは直前の要素から次の要素までの距離でした。
**距離の閾値は絶対値では企画できず、部品の配置からしか決まらない**——並べてから測るしかない。

No.87〜89 は**動きが届かないところ**を3つ集めた回です。
86種は、口に出さないまま3つの前提を置いていました——引き継ぐ相手が同じ画面の中に見えている、
動きを再生してよい、そして**動いている瞬間、誰かが見ている**。3つとも外れる場所があります。

- **No.87 受け皿が画面の外** → 証拠を**残った枠**へ。飛んだものは端に着く前に消え、縁がその一点だけ凹む
- **No.88 動きを再生してはいけない** → 尺と反復は潰し、量は**個数**へ、跳ねは**段階**へ翻訳する
- **No.89 動いた瞬間、誰も見ていない** → 再生ではなく**時間で消えない跡**。既読は時間ではなく行為で決まる

3種に共通する設計則はひとつです。**動きのうち尺と反復は装飾、順序・段階・差分・痕跡は情報。**
動きという担体が使えない場所では、装飾は捨ててよく、情報は必ず別の担体へ移さなければならない。
移す先はいつも「動いたもの」ではなく**残っているもの**の側にあります
（No.87 は縁、No.88 は静止した点の個数、No.89 は消えない跡）。

No.88 は図鑑で初めて `prefers-reduced-motion` そのものを主題にした標本で、
3値トグル（動きあり / 潰すだけ / 翻訳する）の真ん中がグローバルCSSの再現、右がこの標本の主張です。
題材の1つは**わざと対照が負けないように**置いてあります——`transition-delay`（順序＝情報）は
`transition-duration`（尺＝装飾）を潰しても残るので、潰すだけで十分な場面もある。
潰していい境界線がどこにあるかを見分ける作業そのものが、reduced-motion の設計です。

No.89 は No.85 の閾値に続く、図鑑で2例目の「**動きを出さない判断**」になりました。

No.84〜86 は**置き換わるとき、何を引き継ぐか**を3つ集めた回です。
83種はぜんぶ「ひとつの部品の中」で閉じていて、あるものが別のものに入れ替わる語彙を1つも持っていませんでした。
引き継ぐものは3通りあり、引き継ぎ方の答えもそれぞれ違います。

- **No.84 同一性を引き継ぐ** → **消さない**。位置もサイズも変えてよいが、`opacity` を1度でも落としたら別物になる
- **No.85 型を引き継ぐ** → **寸法を先に置く**。骨が実体と同じ型でなければ、置き換わった瞬間に読んでいた場所を失う
- **No.86 引き継ぐものが無い** → **順番で語る**。先に抜き、ひと拍おいてから席を閉じる

3種とも対照が同居しています。No.84 はクロスフェード（**座標が完全に一致していても別物に見える**）、
No.85 は骨の寸法を実体と揃えない版（実測で12/24/36pxずつ行が飛ぶ）、No.86 は抜けと詰まりを同時に走らせる版
（下の行が消えかけの行に乗り上げて「潰した」ように見える）。
どれも**違うのは1箇所だけ**で、その1箇所が意味を丸ごと変えます。

No.85 には図鑑で初めて「**動きを出さない判断**」が入りました。
200ms 以内に返ってきたら骨を出さない、いったん出したら最低400msは消さない——
どちらもアニメーションの設計ではなく、アニメーションを差し控える閾値の設計です。

No.81〜83 は**動きが終わる前に、次の入力が来る**場面を3つ集めた回です。
80種はぜんぶ「ひとつの動きが最後まで再生される」前提で成立していましたが、実際の指はそれを待ちません。
中断には種類があり、正しい答えもそれぞれ違います。

- **No.81 逆向きの入力が来る** → **反転**。いま居る場所から、残りの距離に比例した尺で引き返す
- **No.82 同じ入力が連発する** → **合流**。拍は1つのまま、振幅だけが育つ（回数は数字にしない）
- **No.83 進行中の動きに手が触れる** → **停止**。その場で即。止まりに演出をつけない

3種とも同じ実装の背骨を持っています——**`@keyframes` は中断できない**（0から刻まれた台本なので、
打ち直すと端までワープしてから走り直す）。**`transition` は中断できる**（いまの計算値から補間するので、
`getComputedStyle` で現在位置を読み、`transition: none` で固定してから新しい行き先を張り直せる）。
No.81 と No.83 には対照が同居していて、この差がそのまま目で見えます。

No.73〜75 は**図鑑の基本イージング（ぷるん）が意味の側から使えない場所**を3つ集めた回です。
図鑑は72種にわたって `cubic-bezier(0.34, 1.56, 0.64, 1)` の「行き過ぎて戻る」を共通語彙にしてきましたが、
この3か所ではそれが**使えないのではなく、使ってはいけない**——理由がそれぞれ違います。

- **No.73**：再生ヘッドをユーザーが握っている。予備動作を仕込むと、逆走したとき
  「これから起きること」の予告が「さっき起きたこと」の位置に出て意味が壊れる
- **No.74**：確度の帯が行き過ぎるのは「一瞬だけ実際より確信していた」という、確度についての嘘になる
- **No.75**：緩急そのものが状態（負債の残高）なので、跳ね方は演出ではなく情報になっている

代わりに何が気持ちよさと意味を作るか、が3種の中身です。No.73 は速度由来の誇張だけを使い
（逆走で符号が自然に反転するので対称に壊れない）、No.74 は幅の縮み方だけで語り、
No.75 は跳ね返りの量そのものを目盛りにしました。
No.75 は図鑑で初めて、**前の操作が次の操作の緩急を変える**標本でもあります
（他の74種は全部「その場の1回」で閉じている）。

No.13〜26 は「ゲーム」カテゴリ。ゲームUIの定番フィードバック
（被弾・報酬・成長・待ち時間・緊張・エラー）をCSSだけで演じる標本群です。

No.27〜32 はモバイルゲームの「指の操作」標本群。長押し・ドラッグ・スワイプ・
フリックといったジェスチャーそのものを主役に、入力の深さや勢いが
そのまま画面の手応えになる感触を Pointer Events + CSS で採集しています。
（マウスでもタッチでも同じように触れます）

No.33〜37 はカードスタックの標本群。[Amicro](https://github.com/Subhan-code/Amicro--Micro-transitions-)
のカードUIからスプリング定数・角度・オフセットを採取し、Motion のバネを
CSSのイージングに換算して移植したものです（基本バネ stiffness180/damping20/mass0.8
≒ `transform 0.55s cubic-bezier(0.34, 1.3, 0.64, 1)`）。

No.38〜49 は「アナリティクス」カテゴリ。ダッシュボードのグラフに
「発表の緩急」を持たせる標本群です。棒・折れ線・円弧・点・セル・面・帯・針・数字といった
グラフの部品に、にょきにょき／ひと筆書き／熱伝導／滴り／鼓動といった動きの性格を掛けています。

No.50〜54 は**実プロジェクト側から逆算して足した標本群**です。
経営シミュレーション（RUNWAY 78）のUIを部品単位で数え、「必要としているのに
図鑑に無い動き」を洗い出して作りました。共通するのは、報酬の祝祭でも汎用UIのぷるんでもなく、
**数字が意味を持つ画面で緩急を作るための語彙**という点です。

- **符号を色ではなく向きで語る**（No.50: 増は上へ生え、減は肩からぶら下がる）
- **総量が変わらない中での押し合い**（No.51 / No.54: 誰かが太れば必ず誰かがたわむ）
- **状態ではなく変化だけを鳴らす**（No.52: 越えている間ではなく、越えた瞬間に震える）
- **「選んだ」と「効いた」の間に予約という状態を置く**（No.53: 呼吸 → 押印 → 締め）

No.53 / No.54 は操作そのものが主役なので自己完結の対話標本、
No.50〜52 は下のアナリティクス標本と同じ props 駆動です。

No.55〜58 は同じ棚卸しの続きですが、**すでに苗床にあった種**から
「そのプロジェクトで実際に使う当てがある」ものを選んで育てたぶんです。
No.55 前週比矢印は前期比を出す全タイル、No.56 目標の差し替えは毎週入れ替わる焦点、
No.57 アンロックは解禁条件、No.58 打刻はイベントマーカーに当たります。
種を寝かせておくと、あとで「使う当て」のほうから採りに来られる——という運用の実例です。

No.59〜61 は**「まだ確定していないもの」をどう見せるか**でまとめた3種です。
図鑑にはこれまで「起きたこと」を伝える動きは揃っていましたが、
**これから起きること・まだ動きうること・残り時間**を伝える語彙がありませんでした。
3つに共通するのは、状態をラベルや色で説明せず、**動いているかどうかそのものを意味にした**点です。

- **揺れている＝まだ効いていない / 止まった＝確定した**（No.59: 発効の瞬間は減衰させず1拍で固定する）
- **呼吸の振幅＝確度の低さ**（No.60: 同じ数字でも、振れているほど危うい）
- **落ちる速さ＝焼ける速さ**（No.61: 残量ではなく速度が危機感を運ぶ。そして時間は買い戻せない）

いずれも「確定」を派手な祝祭では祝わず、**動きが止まること**で伝えます。

No.62〜64 は**「探索の語彙」**の標本群です。
この3種を企画した時点の58種でトリガーを数え直すと、ほぼ全部が離散イベント
（クリック・状態変化・データ到着）——つまり「何かが**起きた瞬間**」の語彙しかありませんでした。
けれど実際のダッシュボードで人がいちばん長くいるのは「見ている最中」、
カーソルを彷徨わせて数字を読んでいる時間です。この3種はその時間帯を受け持ちます。

3つに共通させた設計則は次の3点で、**イベント演出とは前提が違う**ところが要点です。

- **可逆であること** — 探索の動きは何十往復もするので「戻り」の緩急まで設計が要る。
  出は速く跳ね、戻りは少し遅く粘る。**戻りを跳ねさせない**（退場は主張しない）
- **主役は強調ではなく減衰で作る** — 触れたものを光らせるのではなく、それ以外を薄める。
  単色の図鑑では特にこれしか手がない。ただし No.64 が示すとおり、
  常時動く画面では**薄めても止めてはいけない**（止めた瞬間「主役でない」ではなく「死んでいる」に見える）
- **カーソルは連続、UIは離散** — 生の座標に追従させるとノイズになる。
  最寄りの意味単位（点・ピース・流路）へ吸着させ、乗り移りだけを補間する

実装上いちばん効いたのは、ホバー状態を `@keyframes` ではなく **`transition` で書く**ことでした。
探索の動きは最後まで再生されないのが常態（隣へ移ると前の動きは戻りきる前に引き返す）なので、
「いま居る場所から補間できる」ことが触り続けられる動きの条件になります。

なお No.63 は No.40 ドーナツの注ぎ込みと同じ部品ですが、**フェーズが違うので別標本**です。
No.40 は「登場」の語彙（弧が注がれて出来上がるまで）、No.63 は「探索」の語彙
（描き終わったあとに触る時間）。同じ部品でも、登場と探索には別々の動きが要ります。

No.65 / No.66 は No.50〜54 と同じ逆算（RUNWAY 78 の週報）から出た2種で、
**同じ場面に対する2通りの答え**として対になっています。

- **No.65 因果のリレー — 集計する側**。原因（投じた資源の配分）と結果（軸ごとの寄与）を
  上下に置き、原因どうしは直列・ひとつの原因が動かした結果は並列に動かします。
  並列に流すと「全部が同時に起きた」に戻ってしまうので、
  **直列と並列の使い分けそのもの**が伝える中身になっています
- **No.66 数値行の物差し — 集計しない側**。単位の違う数字を1つの図にまとめると
  「行をまたいだ長さは比べられない」という但し書きが要りますが、行ごとに物差しを
  持たせれば但し書きそのものが要らなくなります（満幅の意味が行の中で閉じるため）

実プロジェクト側で採用したのは No.66 のほうでした。集計した図は但し書きを1行足す代わりに、
「1枚しか動かしていない軸は必ず半分いっぱい」のような**読み手が知らなくていい約束**を
増やしてしまう——というのが実際に両方を実装して比べた結論です。

No.67〜69 は**「押した時点と、効いた時点のずれ」**の3種です。
それまでの66種はほぼ全部が「操作した瞬間に効く」を前提にしていて、
**操作と効果のあいだに時間が挟まる場面の語彙が1つもありませんでした**。
挟まり方は3通りしかないので、3種でその3通りを1つずつ受け持たせています。

| No. | ずれ方 | 効くのはいつ |
| --- | --- | --- |
| 67 | 効くのを遅らせる（取り消せる猶予） | 猶予が尽きたとき |
| 68 | 効くのが先の話（因果に潜伏期間がある） | 何週か先 |
| 69 | 効いたことにして先に見せる（楽観的更新） | もう効いている…はず |

3種に共通させた設計則は次の3点です。

- **待ち時間を進捗バーで語らない** — 残量ではなく「ほどける」「近づく」「たわむ」で言う。
  No.61（残りの砂）の「速度が量を語る」の続きで、ここでは**形が崩れていく量**と**距離**が時間を語る
- **未確定＝動いている / 確定＝止まる**（No.59〜61 の語彙の継承）。
  ただし **No.69 だけはこの則を意図的に破ります**（下記）
- **戻り道は往路と変える** — 取り消しや訂正を逆再生にすると「取り消した」ではなく
  「最初から何も起きていない」に見え、操作が届かなかったことにユーザーが気づけません。跡を残します

この3種で、図鑑の看板である `cubic-bezier(0.34, 1.56, 0.64, 1)`（ぷるん）が
**使えない場所**が2つはっきりしました。

**No.67 の猶予は linear で刻みます。** 猶予は物理ではなく契約なので、緩急を付けると
残り時間の読み取りが嘘になります（この標本にはオーバーシュートが一箇所もありません）。
あわせて、**ほどけの進行だけは CSS ではなく JS が持っています**。
グローバルCSSの `prefers-reduced-motion` は `animation-duration` を潰すので、
猶予の見た目を CSS アニメーションで書くと、動きを控える設定の人の画面では
**糸が一瞬でほどけ終わったのに確定だけが5秒後に来る**ことになります。
残り時間は装飾ではなく情報なので、減らす対象から外す——
No.61 で見つけた「常時アニメーションの reduced-motion 設計」の、もう一段先の話です。

**No.69 では「未確定＝揺れる」を破りました。** 楽観的更新の目的は待ちを消して
本当らしく見せることなので、未確定の合図を強く出したら本末転倒になります
（揺らした瞬間「失敗しそう」に見え、待たせているのと変わらなくなる）。
合図は縁の薄い破線と1px深い影だけに留め、呼吸は付けていません。
**約束を守るより、その約束が何のためだったかを優先した唯一の標本**です。

No.68 の主題は逆に「**押しても動かないことを、故障ではなく予告に見せる**」ことでした。
押した瞬間に何も動かさないと壊れて見えるので、線の代わりに別のもの——波紋と、
未来側ににじみ出るゴースト——を動かします。ゴーストの座標は、効いたあとに線が通る座標と
一致させてあります。だから到達の瞬間、影は消えるのではなく**線に吸われる**ように見えます。

No.70〜72 は**「向こう側にも時間が流れている」**の3種です。
69種を数え直すと、ほぼ全部が次の3つを前提にしていました——
①操作は1つずつ順に効く ②変化を起こすのは自分だけ ③確定した過去は動かない。
No.67〜69 は①を「ひとつの操作の行方」の範囲で崩しましたが、実装して見えたのは
**その前提自体が現実には保たない**ことでした。人は返事を待たずに次を押すし、
確定したはずの過去はあとから訂正されるし、画面を触っているのは自分ひとりではありません。
残っていた穴はちょうど3つで、1バッチで塞げます。

| No. | 崩す前提 | 一言 |
| --- | --- | --- |
| 70 | 操作は1つずつ効く | 自分の操作が**複数同時に**飛んでいる |
| 71 | 確定した過去は動かない | 時間の向きが**逆**のずれ |
| 72 | 起こすのは自分だけ | 変化を起こすのが**自分ではない** |

3種に共通させた設計則は次の3点です。

- **誰が起こしたかを、動きの強さで語る** — 自分の操作への応答はぷるん（0.28〜0.36s、振幅は大きくてよい）、
  向こうから届いた変化は跳ねない ease-out（0.50〜0.60s、振幅は半分以下）。
  「これは他人の変更です」とラベルで説明しません。図鑑の看板であるオーバーシュートは、
  この3種では**「自分が起こした」の印にだけ**使います
- **待ちと詰まりを長さで語らない**（No.61 / No.67 の継承）。進捗バー・待ち件数・スピナーではなく、
  たわみ・輪郭のズレ・追従の遅れで言う
- **消して差し替えない**（No.67 / No.69 の継承）。旧い値は輪郭やゴーストで 0.8〜0.9s 残す。
  読み手はもう旧い値を読んでいるので、跡が無いと「自分の記憶違い」に見えます

この3種でいちばんはっきりしたのは、**強弱の差そのものが「誰が」を語れる**ことでした。
No.72 は同じ画面に自分のホバー（0.3s のぷるん）と他者の書き換え（0.55s で跳ねない）を並べていますが、
名前も色も出していないのに、どちらが自分の操作かは見れば分かります。
モノクロ制約から「向き＝事実、濃さ＝評価」（No.55）が出たのと同じ筋で、
**主体を表す手段を全部封じると、緩急のほうが主体を語りはじめます**。

もうひとつの収穫は、**割り込みへの答えが2通りある**と分かったことです。
No.54（総量固定の配分）は入らないものを「断る」で答え、No.69 は保留中の再押下を首振りで断りました。
No.70 と No.72 はどちらも断りません——70 は溜めて列にし、72 は順番を譲って待たせます。
断るのは「これ以上は入らない」が本当のときだけで、**ただ順番の問題なら、断りは嘘**になります。

実物を見て直した企画の誤りが2つありました。どちらも仕様書の上では正しく、動かして初めて壊れる類です。

**No.71 の輪郭は前後関係を決め忘れていました。** 下方向の訂正しか頭になかったので背面に置きましたが、
上方向の訂正では旧い縁が新しい実体の「中」に来るため、塗りに飲まれて跡が消えます。
前面に出し、塗りの上に来るときだけ線の色を地の側へ反転させました
（跡が読めることを優先して色は変え、太さ・尺・消え方は変えていません）。

**No.72 のホバーは、強調のつもりが消失になっていました。** 持ち上がりと同時に地を一段暗くする実装で、
その色が図鑑カードの地（`#eaeae8`）とちょうど同じ。触った行だけが背景に溶けます。
持ち上がりは影で言い、地の色は変えない、に直しました。

アナリティクスの12種（No.38〜49）と No.50〜52 は**チャート本体を props 駆動の named export として切り出して**あり、
`data` や `value` が変わった瞬間（＝どんなトリガーでも）その規定値まで描画が走ります：

```tsx
import { SproutingBarsChart } from './specimens/bar-sprout'

// ボタンでも WebSocket でもスクロールでも、props が変われば描き直される
<SproutingBarsChart data={weekly} labels={days} replayKey={replay} />
```

図鑑に並んでいる default export は「その Chart を使う側の見本」で、
ボタンで規定値を差し替えるデモになっています。
ゼロから描き直す標本（棒・折れ線・ドーナツ・散布図・ヒートマップ・ファネル・収支の橋）は
同じデータでの再演用に `replayKey` を、現在値から新しい値へ滑らかに遷移する標本
（ゲージ・KPI・リング・エリア・レーダー・希薄化の帯・しきい値）は連続遷移を受け持ちます。

## 標本を連れて帰る（他プロジェクトでの活用）

アニメーションはすべて **依存ライブラリなしの CSS** です。
各標本は `src/specimens/<id>/` に自己完結しています：

```
src/specimens/toggle/
├── index.tsx   # 最小限の React 状態管理のみ
└── style.css   # 動きの本体。クラスは mz- プレフィックスで衝突しない
```

フォルダごとコピーして `import Toggle from './specimens/toggle'` するだけで動きます。
React 以外のプロジェクトでも、`style.css` のクラスとキーフレームはそのまま流用できます。

図鑑の詳細ビュー（カード右上の ⓘ）から各標本のソースコードを直接コピーすることもできます。

## 動きの設計メモ

- **緩急がキャラクターになる** — `cubic-bezier(0.34, 1.56, 0.64, 1)`（オーバーシュート）を基本のイージングとして共有し、ブランドとしての一貫した「ぷるん」を作る
- **予備動作 → 本動作** — 押すと一度潰れ、離すと跳ねる。溜めがあるから気持ちいい
- **変形は連続で** — ボタンがメニューに「なる」ことで、視線の迷子を防ぐ
- **二次アクションで生命感** — まばたき・呼吸・波紋など、主動作に添える小さな動き
- `prefers-reduced-motion` に配慮済み

## 新しい標本の追加

1. `pnpm new <id>` で `src/specimens/<id>/`（index.tsx + style.css）の雛形を生成（クラスは `mz-<id>-*`）
2. `src/registry.ts` の `specimens` 配列にメタデータ（名前・生態・動きの原理）を追記

これだけで図鑑に自動掲載され、ソースコード表示も効きます。

**配線する前に単体で見たいとき**は [`dev-preview.html`](dev-preview.html) を使います。

```
http://localhost:5173/dev-preview.html?id=<id>
```

`src/specimens/*/index.tsx` を `import.meta.glob` で拾うので、`registry.ts` に1行も足さずに
1種だけを 560×360 のステージで触れます。3種を並列で実装するとき、
**実装側に共有ファイル（`registry.ts`）を触らせないまま実物を見せる**ためのものです。
`vite` の build 入口は `index.html` だけなので、本番の生成物には入りません。

## 標本を収録する（PRに動く証拠を載せる）

動きの標本は静止画では良し悪しが判断できないので、GIF に収録する道具を置いてあります。

```bash
pnpm dev                                              # 別ターミナルで起こしておく
npm i --no-save playwright gifenc pngjs               # 収録用（package.json には入れない）
node tools/capture-specimen.mjs <id> out.gif
```

[`capture.html`](capture.html) が標本1種だけを 560×360 のステージに載せるので、
図鑑のヘッダーや他のカードが写り込みません。
ポインタの動かし方は [`tools/capture-specimen.mjs`](tools/capture-specimen.mjs) の
`CHOREO` に標本ごとに書きます——ホバーの標本は「どう触るか」が中身そのものなので、
撮り方を決めるのは説明を書くのとほとんど同じ作業になります。

図鑑本体の「依存ライブラリなし」を守るため、収録に要る3つは `package.json` に入れず
その場で入れて使います。

## アイデアの苗床（案出しと蓄積のしくみ）

完成した標本とは別に、**思いつきを貯める台帳**が [`src/ideas.ts`](src/ideas.ts) にあります。
1エントリ追記するだけで図鑑下部の「アイデアの苗床」セクションに自動で並び、
`seed（種）→ sprout（育成中）→ captured（標本化済み）` のライフサイクルで管理されます。

- 案出しのレシピ（部品 × 動きの性格 × ゲームの感情 の掛け算）
- 台帳への書き方と、種を標本に育てる手順

は [IDEAS.md](IDEAS.md) にまとめてあります。行き詰まったらそちらへ。
