# 収録基準 — モーション図鑑の憲章

> UIが**生き物のように**動く、感覚のカタログ。

この図鑑が採集するのは「動きの**性格**」です。UI部品やテーマ（ゲーム・モバイル・カード…）は、
その性格を見せるための**器**にすぎません。新しい標本を足すとき・既存の標本を直すときは、
必ずこの基準で自己審査してください。器が変わるたびに少しずつ原点から離れていくのを防ぐための文書です。

## 合格の3条件（すべて必須）

### 1. 性格をオノマトペ1語で言える

「ぷるん」「ぐしゃっ」「そわそわ」「よいしょ」「ぺたっ」——言えない動きは標本ではなく**技法**です。
その1語が `ideas.ts` の `motion` と、`registry.ts` の生態の中心になります。

### 2. 「体」の原則を1つ以上、合計2つ以上持つ

| 体の原則（1つ以上必須） | 時間の原則 |
| --- | --- |
| スカッシュ&ストレッチ（潰れる・伸びる・しなる） | オーバーシュート |
| 予備動作（ためる・沈む・引く） | 時間差（フォロースルー・重なり） |
| 二次アクション（まばたき・震え・揺れ） | 減衰振動 |
| 待機中の呼吸（触られていない時も生きている） | じらし・緩急 |

**オーバーシュートの `transition` だけで状態Aと状態Bをつなぐのは不合格**です。
それはレイアウトのアニメーションであって、生き物ではありません。
最低でも `@keyframes` で「体」が一度は変形するか、仕草をすること。

### 3. 生態は「何を感じ、どう振る舞うか」で書く

生態（`ecology`）の主語は標本自身。「待ちきれずにそわそわする」「着地でぺたっと落ち着く」のように、
**気持ちと仕草**で書く。

- 角度・px・バネ定数などの**数値**は CSS のコメントへ。生態には書かない
- 元ネタ・出典・オマージュは**一言添える程度**に留め、主語にしない
- 原則タグ（`principles`）は**動きの言葉**で。
  ✕「扇形展開」「奥行き」「入力の可視化」「しきい値の可視化」
  ○「時間差の展開」「着地のしなり」「追従と遅れ」「ためと解放」

## 実装の約束

- 依存なしの CSS。クラスは `mz-<id>-*`。フォルダ単体で自己完結
- 基本イージングは `cubic-bezier(0.34, 1.56, 0.64, 1)`（ぷるん）。弱める（1.3〜1.4）のは可だが、
  理由を**性格の言葉で**コメントに書く（「元ネタがそうだから」は理由にならない）
- 位置は `transform`、体の変形は個別プロパティ `scale` / `translate` の `@keyframes` に分けると、
  配置と仕草が喧嘩しない
- 初回表示でいきなり閉じる系のアニメが走らないよう、`is-anim`（一度触ったら付くクラス）で守る
- `prefers-reduced-motion` はグローバル CSS が面倒を見る

## ドリフトの実例（こうなったら軌道修正）

図鑑は No.33〜37（カードスタック）で一度大きく原点を外れ、改修されました。そのときの症状：

| 症状 | どこが外れているか |
| --- | --- |
| `@keyframes` が1つもなく、最終配置へ `transition` するだけ | 条件2：体がない |
| 原則タグが「扇形展開」「不均等配置」「奥行き」 | 条件3：配置の言葉になっている |
| 生態が「±38°」「+6px」「iPodへのオマージュ」 | 条件3：数値と出典が主語 |
| 外部ライブラリの値に合わせてイージングを変えた | 実装の約束：移植が目的化 |

指操作の標本（No.27〜32）では、見どころが「しきい値」「軌道の予告」といった**入力の仕組み**に
寄りかけました。仕組みは舞台装置、主役はそれに反応する**生き物**です。

さらに大きなドリフトが、自動ルーチンの積み上げ（No.59以降、特に No.90〜188）で起きました：

| 症状 | どこが外れているか |
| --- | --- |
| 標本名が「さかのぼり訂正」「何もしなかったことで閉じる」のような**問い・命題** | 条件1：オノマトペで言えない |
| 既定が「`0s` / `none`、20フレームの差分 0px」＝**動かないことが正解** | 条件2：図鑑の存在理由の否定 |
| 生態が「distinct 1値」「禁止語0件」「336×170px」の**計測表** | 条件3：気持ちと仕草が消えた |
| 次の企画が「No.183 がこう決めたので…」と**前回の結論から導出** | 原点ではなく直前の自分を基準にしている |

**企画の根拠は、常に初期標本と「触って気持ちいいか」に置く。** 直前の標本の結論から次を導かない。

## 新しい器（シリーズ）を持ち込むとき

1. 器のテーマから入らず、まず「その器に宿したい性格」を3つオノマトペで書く
2. 初期標本（No.01〜12）と横に並べて、同じ図鑑に見えるかを確かめる
3. 外部の作品から採取するのは**動機**（何が気持ちいいか）であって、数値ではない
